# 产品需求文档

> 版本：3.0
>
> 更新日期：2026-09-23
>
> 本文档是产品行为与需求边界的唯一权威来源。实现状态见 [PROJECT_PLAN.md](PROJECT_PLAN.md)，技术实现见 [ARCHITECTURE.md](ARCHITECTURE.md)。

## 1. 产品定义

### 1.1 一句话定位

为个人创作者提供一个本地优先、批量高效、可解释的图片/视频素材库，让素材“进得来、找得到、打得快、拿得走”。

### 1.2 目标用户

- 摄影师、设计师和自媒体创作者。
- 主要管理数千到数万张本地图片、视频和 RAW 文件。
- 需要中文搜索、标签治理、批量 AI 辅助和本地数据安全。

### 1.3 设计原则

1. **本地优先**：素材库、标签和索引默认保存在本机。
2. **人机协作**：AI 只生成建议，用户确认后才写入正式标签。
3. **任务可控**：长任务显示真实阶段和进度，可取消，可恢复，不静默截断。
4. **结果可解释**：搜索、失败、删除和 AI 降级必须让用户知道发生了什么。
5. **数据安全**：删除、移动、恢复和备份均有明确边界，禁止假装成功。
6. **界面克制**：内容优先，减少无关入口、重复信息和装饰性 UI。

### 1.4 平台支持边界

首发目标为 Windows 10/11 x64 完整功能、Apple Silicon macOS 与 Ubuntu 24.04 x64 知情测试预览。平台能力差异、支持等级、暂不支持项和真机验收门槛统一以 [PLATFORM.md](PLATFORM.md) 为准；本 PRD 不重复维护平台矩阵。平台构建成功不等于该平台已达到预览或正式支持等级。

## 2. 信息架构

### 2.1 主导航

底栏主入口固定为：

- 入库
- 素材库
- 打标

设置页从素材库进入。超级搜索可通过双击“素材库”进入，悬停或键盘聚焦时显示明确提示。

### 2.2 核心页面

| 页面 | 职责 |
|---|---|
| 入库页 | 选择、暂存、检查、改名并托管导入素材 |
| 素材库 | 浏览、筛选、搜索、批量操作、查看详情与回收站 |
| 超级搜索 | 使用必须、优先、排除、元数据和 AI 条件组合检索 |
| 打标页 | 查看批次、运行 AI、人工确认、编辑标签和撤销批次 |
| 设置页 | AI 连接、本地模型、入库与库位置、外观、数据与缓存、备份恢复 |
| 查看器 | 全屏浏览图片/视频、切换素材、评分、收藏、标签和元数据 |

## 3. 功能需求

状态说明：

- **已实现**：代码链路存在并已接入 UI。
- **需真机/UAT**：实现存在，但必须在目标桌面环境和真实数据上验收。
- **暂缓**：保留产品方向，不在当前发布范围。
- **远期**：需要新架构或外部条件。

### 3.1 入库与文件托管

| ID | 需求 | 状态 |
|---|---|---|
| R-01 | 文件和文件夹批量选择/拖拽；先进入待入库清单，确认后执行 | 已实现 |
| R-02 | 入库前显示图片/视频数量、大小、重复和失败明细；无法生成入库缩略图的图片必须在进入队列前明确列出并排除，支持预览但后续高清/元数据能力受限的特殊格式必须显示警告 | 已实现 |
| R-03 | 支持是否托管；托管时复制到总库/分库，分库名禁止路径穿越 | 已实现 |
| R-04 | 支持改名模板 `{分库}`、`{原名}`、`{日期}`、`{序号:N}`，实时预览 | 已实现 |
| R-05 | 重复文件识别，重复项不静默覆盖 | 已实现 |
| R-06 | 连续追加文件；任务可取消；部分成功必须如实汇总 | 已实现 |
| R-07 | 入库占位图优先使用内嵌预览或轻量生成，不阻塞后续浏览；执行导入前再次校验禁止 IPC 绕过不支持预览的文件 | 需真机/UAT |

### 3.2 素材库与查看器

| ID | 需求 | 状态 |
|---|---|---|
| R-10 | 虚拟滚动网格，支持图片、视频和未打标筛选 | 已实现 |
| R-11 | 单选、Ctrl 多选、Shift 范围选、全选、反选和取消选中 | 已实现 |
| R-12 | 右键菜单、打开所在文件夹、复制路径和外部打开 | 已实现 |
| R-13 | 按创建时间、拍摄时间、大小、分辨率、评分等排序 | 已实现 |
| R-14 | 多标签组合筛选，支持 any/all 语义 | 已实现 |
| R-15 | 全屏查看器支持图片缩放/平移、视频播放、前后切换和元数据查看 | 需真机/UAT |
| R-16 | 1-5 星评分、清空评分和收藏状态 | 需真机/UAT |
| R-17 | 图片主色色板和数值/技术特征可参与检索 | 已实现 |
| R-18 | 图片悬浮预览、视频受控悬浮播放和失败回退 | 需真机/UAT |

### 3.3 搜索

| ID | 需求 | 状态 |
|---|---|---|
| R-20 | 普通素材库搜索支持文件名、标签和中文 FTS5 检索 | 已实现 |
| R-21 | 短中文查询、ASCII 子串、CJK 与数字字母粘连均有明确兜底 | 已实现 |
| R-22 | 超级搜索支持必须、优先、排除三区 | 已实现 |
| R-23 | 超级搜索支持嵌套布尔表达式、元数据和数值范围 | 已实现 |
| R-24 | 优先区顺序代表优先级；UI 不暴露权重；执行层按位置归一化 | 已实现 |
| R-25 | 超级搜索提供逐条件诊断；归零时使用可理解文案 | 已实现 |
| R-26 | AI 智能搜只输出受限意图结构，不生成 SQL、tagId 或分页 | 已实现 |
| R-27 | AI 解析失败时按设计降级到部分理解或关键词搜索；配置错误必须明确报错 | 已实现 |
| R-28 | 3 万素材下普通搜索目标响应 ≤500ms，内部体感目标 <300ms | 需真实数据集/UAT |

搜索执行细节以 [contracts/search-plan-v3.md](contracts/search-plan-v3.md) 和 [contracts/super-search-ai-v2.md](contracts/super-search-ai-v2.md) 为准。

### 3.4 标签与分面

| ID | 需求 | 状态 |
|---|---|---|
| R-30 | 标签支持父子层级、递归筛选和安全移动，禁止环 | 已实现 |
| R-31 | 分面是稳定机器协议，key 创建后不可修改 | 已实现 |
| R-32 | 分面区分 `ai_and_manual` 与 `manual_only` | 已实现 |
| R-33 | 系统分面不可删除；自定义分面可管理、停用和恢复 | 已实现 |
| R-34 | 支持标签别名、规范化、合并、重命名和候选搜索 | 已实现 |
| R-35 | 删除或合并标签必须保持素材关联、FTS 和计数一致 | 已实现 |
| R-36 | 支持数值分面的范围、排序、手工赋值和 AI 数值建议 | 已实现 |
| R-37 | 批量打标支持撤销，且不得误删后续手工标签 | 已实现 |

分面规则以 [contracts/facets-v2.md](contracts/facets-v2.md) 为准。

### 3.5 AI 打标

| ID | 需求 | 状态 |
|---|---|---|
| R-40 | 支持多个 AI 连接档案和用途绑定 | 已实现 |
| R-41 | 支持 OpenAI 兼容接口和 Anthropic 接口 | 需真实服务/UAT |
| R-42 | API Key 存系统凭据，不写明文设置 JSON | 已实现 |
| R-43 | 支持云端和 Ollama 本地模型的统一打标管线 | 需真实模型/UAT |
| R-44 | 打标工作台不区分 AI/手动模式；所有批次都可直接手工填写，也可随时生成 AI 建议 | 已实现 |
| R-45 | AI 建议按分面生成；一句话描述包含主体、动作/状态和环境，目标 12–30 字；主体对象最多 3 个，人物在主体中统一写「人」，场景地点最多 3 个并按空间/环境/地点多值输出；人物属性最多 8 个，按人物状态、人数档位、性别、年龄段、穿着和动作分别输出，混合人群不写复合词；主体为空但描述有明确视觉锚点时补调一次，确实无主体时保留空值并由工作台显示「未识别」；人物状态区分有人、无人、未知，只有确认无人且置信度达标才建议「无人」；核心分面 AI 新词可经人工确认写入并归入「其他」，其他建议同样等待确认；低于最低置信度的标签直接拦截；单条解析失败只影响该素材 | 已实现 |
| R-46 | 支持批次进度、取消、中断续跑、单条确认、全部确认和拒绝恢复 | 需真机/UAT |
| R-47 | 视频可抽头/中/尾三帧，按多帧证据合并标签 | 需真实视频/UAT |
| R-48 | 本地模型支持 Ollama 检测、安装、启动、显存推荐和模型拉取 | 需真机/UAT |
| R-49 | 每个 AI 连接可配置最大并发、每分钟和每小时请求数；留空或 0 表示不限。打标与 AI 搜索共享同一连接限额，实际发送的请求（含重试）计数；服务返回 HTTP 429 时停止当前批次并保留未处理项，不自动继续轰击服务 | 已实现，需额度/并发 UAT |

### 3.6 删除、回收站和去重

| ID | 需求 | 状态 |
|---|---|---|
| R-50 | 删除提供“仅移出库”和“删除文件”两种明确策略 | 已实现 |
| R-51 | 仅移出库进入回收站，可恢复；超期清理可配置 | 已实现 |
| R-52 | 删除文件失败时保留数据库记录并明确报错 | 已实现 |
| R-53 | 支持文件哈希重复检测 | 已实现 |
| R-54 | 支持感知哈希相似图检测和同源文件组 | 需真实样本/UAT |
| R-55 | 批量处理重复项时不得误删保留项 | 已实现 |

### 3.7 导出与数据安全

| ID | 需求 | 状态 |
|---|---|---|
| R-60 | 本地复制/移动导出，文件名冲突自动避让 | 已实现 |
| R-61 | move 导出成功后同步库记录，禁止产生死路径 | 已实现 |
| R-62 | 支持 flat、按标签、按日期布局 | 已实现 |
| R-63 | 支持 UTF-8 BOM CSV 清单 | 已实现 |
| R-64 | 支持数据库备份、校验和恢复；恢复前保留旧库 | 需真机/UAT |
| R-65 | 运行日志滚动保存并可打开日志目录 | 已实现 |

### 3.8 外观与设置

| ID | 需求 | 状态 |
|---|---|---|
| R-70 | light/dark/system 主题，组件只依赖语义令牌 | 已实现 |
| R-71 | 设置页展示数据目录、缓存、日志和本地模型占用 | 已实现 |
| R-72 | 清缓存、重置数据、恢复备份等危险操作必须确认 | 已实现 |
| R-73 | “诊断与支持”提供使用帮助入口，并通过系统默认浏览器打开帮助文档 | 已实现 |

## 4. 非功能需求

### 4.1 性能

- 3 万素材网格滚动无明显卡顿和白块。
- 普通搜索目标 ≤500ms，内部体感目标 <300ms。
- JPG 320px 缩略图目标 ≤5ms，RW2 320px 目标 ≤100ms。
- 高清 RAW 解码必须异步、限流并放在数据库锁外。
- 数据库锁内禁止网络、解码和大文件 IO。

### 4.2 稳定性

- 迁移必须可重入、可恢复，已发布版本不得回改。
- 崩溃或重启后，遗留 AI 批次不得永久停在 processing。
- 备份恢复必须拒绝与运行中的导入、导出或打标冲突。
- 失败写入不得伪装成功。

### 4.3 安全与隐私

- 本地素材和索引默认不上传。
- 云端 AI 只发送用户明确触发处理的素材预览。
- API Key、token 等凭据进入系统凭据库。
- 路径操作必须防止目录穿越；SQL 必须参数绑定。

### 4.4 可用性

- 1366px 和 1920px 宽度无内容重叠。
- 所有图标按钮有可访问名称或 tooltip。
- 选中、错误、禁用状态不能只依赖颜色。
- 关键操作可用键盘完成。

## 5. 当前不做的范围

| 方向 | 决策 |
|---|---|
| 百度网盘/夸克网盘导出 | 暂缓，外部接口或资质条件未满足 |
| 目录自动监控 | 暂缓，先保证显式入库链路稳定 |
| 音频素材 | 远期 |
| 多设备同步和账号体系 | 远期，与纯单机本地优先定位存在冲突 |
| CLIP 语义向量搜索 | 远期，需模型、存储和迁移方案完整设计 |
| AVIF/JPEG XL | 在解码依赖、体积和分发风险明确后再评估 |

## 6. 发布验收

发布前必须满足：

1. `scripts/smoke.ps1` 完整通过。
2. 自动化质量门禁全部通过。
3. 使用真实素材完成 [QA_PLAYBOOK.md](QA_PLAYBOOK.md) 中 P0 场景。
4. 超级搜索 relevance、mustNot、位置权重和 hydrate 行为有真实数据证据。
5. 导入、查看器、AI 打标、导出、删除恢复、备份恢复主链路无 P0/P1 未解决缺陷。
6. 当前版本的数据迁移、备份和恢复路径已验证。

## 7. 产品不变量

1. 素材文件不得因 UI 假成功而丢失。
2. AI 不得绕过人工确认直接改写正式标签。
3. 搜索条件只有一个执行计划，不允许列表、总数、全选和诊断各走一套逻辑。
4. 分面 key 是机器协议，展示名不是协议。
5. 任何“删除”和“移动”都必须能解释最终文件和数据库状态。
6. 任何降级结果都必须让用户看见原因，不能静默变成另一条查询或空结果。
7. 用户已保存的搜索计划、标签、评分和回收站状态必须跨重启保持。
