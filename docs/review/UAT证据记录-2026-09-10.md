# 超级搜索发布前 UAT 证据记录

日期：2026-09-10  
仓库：`F:\vibecode\chabaosucai`  
判定：自动化回归通过；真实素材语义 UAT 待补，不作为放行证据。

## 已执行

| 场景 | 结果 | 证据 |
|---|---|---|
| 素材库单击 | 通过：页面仍为素材库，未出现超级搜索标题 | Vite 页面 DOM 快照，`singleSearch=0` |
| 素材库双击 | 通过：出现“超级搜索”标题，底栏仍高亮素材库 | Vite 页面 DOM 快照，`doubleSearch=1` |
| 悬停提示 | 通过：观察到 role=tooltip，文案为“双击进入超级搜索” | `BottomBar.test.tsx` 精确假时钟测试；浏览器观察结果 |
| 键盘 focus/blur | 通过 | `BottomBar.test.tsx` |
| 前端全量测试 | 通过：72 文件，623 passed，2 skipped | `npx vitest run` |
| Rust 质量门禁 | 通过：fmt、严格 clippy、cargo test | PowerShell 命令输出 |

## 当前限制

Vite 浏览器页面未加载 Tauri IPC bridge，出现 `invoke/transformCallback` 既有运行时错误；这不代表桌面 Tauri 应用错误。当前本机素材库为 0 项，也没有准备只命中 should A/B、同时命中、均不命中及 mustNot 命中的真实素材。因此以下证据不能伪造或用空库替代：

1. 五类素材的 ID 顺序与预期语义。
2. `ranking=relevance` 下位置权重改变结果顺序。
3. mustNot 排除和 should 均不命中仍保留。
4. AI replace/append、重启 hydrate 后 plan JSON 的 `minimumShouldMatch=0` 与 `2/1/0.5` 权重。

## 桌面复核步骤

在真实 Tauri 桌面环境准备最小素材集后，按 `docs/review/AI修复指导计划书-最终核验版-2026-09-09.md` 第 10 节执行，并保存：素材 ID/字段表、请求或存储中的 plan JSON、relevance 结果 ID 顺序，以及入口和优先区截图。全部证据齐全前，发布状态保持“不放行”。
