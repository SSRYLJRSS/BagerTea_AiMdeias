# 阶段报告：阶段 0 基线、证据和可观测性准备

## 阶段
阶段 0：基线、证据和可观测性（指导书 §4）

## 本阶段目标
记录工作区基线、验证门禁命令、确认资源路径排除项、为后续阶段固定施工起点。

## 基线

### git status（记录到的未提交修改，不得回退/覆盖/格式化）
工作区存在大量未提交修改，均为前序开发遗留，本阶段全部保留：

- 修改：package.json / package-lock.json、src-tauri Cargo.toml/Cargo.lock、多个 Rust 模块（ai_cmd、super_search_cmd、tags_cmd、assets、migrations、settings、tag_facets、thumbnail、video、lib.rs、state.rs 等）、多个前端文件（Thumbnail、ViewerPage、SettingsPage、settingsStore、tagStore.test、types 等）。
- 删除：src/components/library/VideoPlayer.tsx（已迁移至 src/components/media/VideoPlayer.tsx）、src-tauri/src/db/cloud.rs。
- 新增（未跟踪）：docs/development/、docs/review/（含本指导书与排查报告）、ai_connections_cmd.rs、media_cmd.rs、video_cmd.rs、ai_connections.rs、video_proxy.rs(service/db)、credentials.rs、media_refill.rs、src/api/connections.ts、src/api/video.ts、大量新增组件与测试（AssetInfoPanel、VideoControls、AiConnectionManager、FacetManagePanel、viewer/*、thumbnailCache.ts、mediaMeta.ts 等）。

### 验证命令
| 命令 | 结果 |
|---|---|
| `npm run typecheck` | ✅ 通过 |
| `npm run test:unit` | ✅ 通过（35 个测试文件，187 个测试） |
| `npm run build` | ✅ 通过（1924 模块，8.68s） |
| `cargo check --manifest-path src-tauri/Cargo.toml` | ✅ 通过（EXIT=0） |

### 资源路径排除项（§4.3，仅排除项不是首因）
- `dist/index.html` 使用绝对根路径 `/assets/index-*.js` / `/assets/index-*.css`。
- `tauri.conf.json` frontendDist 指向 `../dist`（标准配置）。
- 结论：Tauri v2 通过 `http://tauri.localhost/` 根路径提供 frontendDist，绝对路径在生产包正常解析（独立评判 §2.5 已确认）。本阶段**不改** vite base / tauri 资源配置，保留为 5 分钟排除项——打包后 WebView2 控制台确认 404 才动。

## 旧报告结论
排查报告（08-27）把 `/assets/...` 资源路径列为 P0 首因；独立评判已纠正：这是未验证假设，应降级为排除项。本阶段按指导书 §0.3 执行：先测 WebView2 首帧/窗口背景/Rust setup 同步耗时，不直接改生产路径。

## 当前代码事实
- `AssetCard.tsx` 仍挂载 `useHoverIntent` + `VideoHoverOverlay` + `MediaPreviewPopover`（阶段 2 待删）。
- `LibraryPage.tsx` 仍把 `ViewerPage` 作为兄弟节点追加渲染，BottomBar 无条件存在（阶段 3 待改）。
- `Thumbnail.tsx` hdUrl/hdReady 仍是组件本地 state（阶段 2 待改为稳定资源策略）。
- 后端 thumbnail.rs 已有 single-flight + tmp→rename 原子写 + LRU 锁外清理（核对通过，不改写，只补测试）。
- settingsStore.load() 尚无 single-flight（阶段 1 待补）。
- lib.rs setup 仍同步执行 LRU 清理（阶段 1 待移出关键路径）。

## 差异原因
需求变更 + 工作区未提交改动并存：08-27 用户明确禁止素材库 hover 预览（替代 08-26 阶段 F 悬浮预览设计）；工作区已有大量前序重构未提交，本阶段不触碰。

## 修改文件
- 新增 `docs/review/阶段报告-2026-08-27-阶段0-基线.md`（本文件）。

## 未修改文件及原因
全部业务代码未改：阶段 0 只做基线记录与验证，不动施工对象。

## 数据迁移
无。

## 新增 API/命令
无。

## 新增测试
无（基线测试 187 个全通过）。

## 验证命令及结果
见上表，四项门禁通过。

## 手工验证步骤（后续阶段补运行态）
1. `npm run dev` 启动一次记录首帧/素材列表时间。
2. `tauri build` 后启动生产包检查 WebView2 控制台 404。
3. 真实素材快速滚动 30 秒录屏。
4. 双击进入 Viewer 确认库页侧栏卸载。
5. 设置 → AI 设置检查说明文字压线。

## 已知风险
- 工作区未提交改动多，阶段提交时需小心区分本阶段改动与前序遗留。
- 环境为沙箱受限（esbuild 子进程 EPERM），vitest/build 需在危险模式（danger-full-access）下运行；cargo 不受影响。

## 回滚方式
本阶段无代码改动，无回滚需求。

## 下一阶段
阶段 1：启动白屏和首屏性能（P0）——启动埋点 + StartupSkeleton + settingsStore.load single-flight + lib.rs setup 后台化。