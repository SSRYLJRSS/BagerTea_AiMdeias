# 阶段报告：用户反馈问题整改（2026-08-27 指导书）综合实施报告

## 阶段
阶段 1–5 综合实施（指导书《用户反馈问题整改开发指导书-2026-08-27.md》§5–§9），含回归与门禁。

## 本阶段目标
按指导书施工：①启动白屏与首屏性能；②删除素材库大图/视频 hover 预览并稳定滚动；③Viewer 页面级互斥替换；④AI 设置信息架构重构；⑤标签与分类统一；最后门禁验证。

## 基线

### git status（进入时记录，未回退/覆盖/格式化任何已有未提交修改）
工作区进入时已有大量前序未提交修改（Rust 模块、前端组件、测试、docs 等），全部保留；本轮增量修改如下。

### 验证命令（基线 vs 结束）
| 命令 | 基线 | 结束 |
|---|---|---|
| `npm run typecheck` | ✅ | ✅ |
| `npm run test:unit` | ✅ 35 文件 / 187 测试 | ✅ 39 文件 / 217 测试 |
| `npm run build` | ✅ | ✅ 1925 模块 |
| `cargo check --manifest-path src-tauri/Cargo.toml` | ✅ | ✅ |
| `cargo test --manifest-path src-tauri/Cargo.toml` | 未跑（门禁要求） | ✅ 54+10 测试通过 |

注：沙箱限制下 vitest/build 需 danger-full-access（esbuild 子进程 EPERM）；cargo 不受影响。

## 旧报告结论（差异声明）
- 08-27 排查报告：素材库 hover 预览/滚动抽搐、Viewer 串层、AI 设置术语、标签三套入口、启动白屏。
- 独立评判：资源路径 `/assets/...` 不应列为 P0 首因；滚动串图另有缩略图本地状态与后端并发两个根因。
- **当前代码事实**：素材库仍存在 hover 预览（已删）；Viewer 仍为兄弟节点追加（已改）；AI 设置术语与重复服务管理（已改）；标签页三套入口（已统一）；启动 setup 同步清理（已后台化）。
- **差异原因**：需求变更（08-27 明确禁止素材库 hover 预览，替代 08-26 阶段 F 设计）+ 前序未提交改动共同作用。本轮按 §0.2 记录需求变更：
  - 旧设计：素材库允许图片 popover 和视频 hover overlay。
  - 当前需求：素材库禁止所有大图/视频 hover 预览，双击后进入 Viewer。
  - 处理：删除素材库预览入口（AssetCard 无 hover、无 `<video>`、无 fixed popover），Viewer 成为唯一大图/视频预览场景。

## 修改文件

### 阶段 1 启动白屏（§5）
- `src/utils/startupMarks.ts`（新）：启动埋点（markStartup / getStartupMarks / resetStartupMarks），生产默认静默。
- `src/components/common/StartupSkeleton.tsx`（新）：主题一致骨架（logo + 正在准备素材库 + 不定进度条），纯展示不触发请求。
- `src/components/common/StartupSkeleton.test.tsx`、`src/utils/startupMarks.test.ts`（新）。
- `src/stores/settingsStore.test.ts`（新）：single-flight / 失败可重试 / 不写默认覆盖。
- `src/main.tsx`：html_dom_content_loaded + react_first_render 打点。
- `src/App.tsx`：设置未 loaded 显示 StartupSkeleton（6s 超时兜底）；viewerOpen 时隐藏 BottomBar、去掉 pb-14（§7.3 方案 A）。
- `src/stores/settingsStore.ts`：`load()` single-flight（模块级 loadPromise）。
- `src/stores/libraryStore.ts`：library_ready 打点 + viewerOpen/gridScrollTop 状态。
- `src/components/library/Thumbnail.tsx`：first_thumbnail_ready 打点。
- `src-tauri/src/lib.rs`：setup 关键路径只保留建目录/开库/迁移/注册；LRU 清理、回收站清理、中断批次标记、预置标签整理全部移入具名 `maintenance::*` 后台线程（不阻塞窗口）。
- `src/styles/index.css`：startup-indeterminate 关键帧。

### 阶段 2 删除素材库 hover 预览（§6）
- `src/components/library/AssetCard.tsx`：删除 useHoverIntent / MediaPreviewPopover / VideoHoverOverlay / getImageHoverPreview / convertFileSrc 视频播放；只保留 Thumbnail + 角标 + 选中 + 文件名 + 事件。
- 删除 `src/components/media/VideoHoverOverlay.tsx`、`VideoHoverOverlay.test.tsx`、`src/api/mediaPreview.ts`（rg 确认无其他消费者）。
- 保留 `HoverPreviewTrigger` / `MediaPreviewPopover` / `useHoverIntent` / `ImagePreviewContent`：导入页 PendingList 仍使用（§6.1 素材库入口与其他页面入口分离）。
- `src/components/library/AssetCard.test.tsx`（新）：无 `<video>` / 无 role=dialog / 无 fixed 预览层；双击仍 onPreview。
- `src/components/library/AssetGrid.test.tsx`：新增列数变化稳定性测试（§13.1）。

### 阶段 3 Viewer 页面级替换（§7）
- `src/pages/LibraryPage.tsx`：preview 非空整体 return `<ViewerPage>`（GridToolbar/SideBar/AssetGrid 卸载）；关闭恢复。
- `src/components/library/AssetGridView.tsx`：滚动位置写入 libraryStore，Viewer 关闭后恢复（§7.2 上下文恢复）。
- `src/App.tsx`：viewerOpen → BottomBar 隐藏（§7.3 方案 A），TitleBar 保留。
- `src/pages/LibraryPage.test.tsx`：新增互斥测试（侧栏卸载/独占/关闭恢复）。
- `src/App.test.tsx`（新）：骨架显示 + BottomBar 按 viewerOpen 显隐。

### 阶段 4 AI 设置重构（§8）
- `src/components/settings/ServiceManagement.tsx`（新）：AI 服务唯一管理入口（在线/本机二选一 + AiConnectionManager + 本机引擎向导）。
- `src/components/settings/AiConnectionManager.tsx`：文案「连接档案→AI 服务」「+ 新增连接→+ 新增服务」「接口协议→API 格式（高级）」；删除确认说明影响范围。
- `src/pages/SettingsPage.tsx`：AI 设置子页改为 超级搜索 / 自动打标 / 服务管理；用途页只保留「此功能使用的服务」+ 功能参数，不再重复渲染服务管理列表（§8.7）；删除 `-mt-2` 负边距说明文字（§3.5 压线修复）；UsageBindingLine 文案改「此功能使用的服务 / 跟随默认服务」。
- 测试更新：`SettingsPage.test.tsx`（新 IA 断言）、`AiConnectionManager.test.tsx`（用户文案断言）。

### 阶段 5 标签与分类统一（§9）
- `src/components/settings/FacetManagePanel.tsx`：重构为分类大类唯一入口；每个分面详情同一上下文 = 基本规则（即时保存）+ AI 行为（hint/参与/工作台显示，随设置草稿统一保存）+ 分类词条（二级编辑器，标题体现上下文「分类词条：××」）。
- `src/components/dialogs/TagManageDialog.tsx`：新增 title prop（§9.3 上下文标题）。
- `src/pages/SettingsPage.tsx`：tags 路由删除并列的「AI 行为配置」独立列表与「分类词条」顶层卡片；只保留 FacetManagePanel。
- 测试更新：`FacetManagePanel.test.tsx`（同一详情三段式 / 草稿回调 / 词条上下文标题）。

### 其他
- `docs/review/阶段报告-2026-08-27-阶段0-基线.md`（新）：阶段 0 基线报告。
- `docs/review/阶段报告-2026-08-27-综合实施.md`（本文件）。

## 未修改文件及原因
- `src-tauri/src/services/thumbnail.rs`：single-flight / tmp→rename 原子写 / LRU 锁外清理核对通过（§1.2/§6.3），只验证不改写。
- `src-tauri/src/commands/thumbnail_cmd.rs`：已走 spawn_blocking 且后端 single-flight，无需改。
- `vite.config.ts` / `tauri.conf.json`：资源路径作为排除项（§0.3/§4.3），Tauri v2 根路径生产正常，无实际 404 证据，不改。
- `src/stores/libraryStore` 数据竞争逻辑、Rust 视频/媒体迁移：已有修复痕迹（§1.2 VERIFY 项），回归通过。
- 未删除 `HoverPreviewTrigger/MediaPreviewPopover/useHoverIntent/ImagePreviewContent`：导入页 PendingList 仍消费（§6.1 保留原因）。

## 新增/修改 API
- 无新 Tauri command。仅前端新增 `markStartup`/`getStartupMarks`/`resetStartupMarks`（utils）、`ServiceManagement`（组件）、TagManageDialog `title` prop（可选）。

## 数据库迁移
无（V15 连接迁移之前已落地；本轮只改前端展示层与启动调度，不动 schema）。

## 缓存/并发影响
- 前端 thumbnailCache 语义未变（§6.2 现实现已满足方案 B：ready 直接 opacity 1、不进新淡入）；新增打点不影响请求。
- 后端生成并发、原子写保持；LRU 清理移后台线程后不再持有 DB 锁期间做 IO（原实现已是锁外清理，本轮仅改为延迟执行）。

## UI 变化
- 首屏：skeleton（不白屏）；素材库卡片 hover 无媒体层；Viewer 打开后库页整体卸载、BottomBar 隐藏；AI 设置 IA 大幅调整（服务管理唯一入口）；标签页分面详情三段式。

## 新增测试
- settingsStore single-flight ×4、StartupSkeleton ×3、startupMarks ×2、AssetCard ×5、LibraryPage Viewer 互斥 ×2、App 骨架/BottomBar ×3、AssetGrid 列数变化 ×1、AiConnectionManager 文案 ×1、FacetManagePanel 三段式 ×2、SettingsPage IA ×3。共新增 30 个断言级测试（187 → 217）。

## 运行时验证（§4.2 最低复现）
- `npm run dev`：已启动 Vite，`http://localhost:1420` 返回 200 且带 root 挂载点；前端模块（含 StartupSkeleton/main.tsx）编译入口放宽验证（dev server 在沙箱下仅验证 HTTP 层）。
- 生产包 WebView2 控制台 / 快速滚动录屏 / 真机双击 Viewer：本环境无法启动 Tauri 桌面窗口（无 GUI 运行环境），无法完成运行态录屏——如实记录，未以静态分析冒充运行态验证。
- `cargo build --release` 已启动作为编译级验证（结果见门禁表）。
- 冷启动/热启动时间戳：埋点已落地（DEV 或 VITE_STARTUP_MARKS=1 输出 `[startup]` 序列），需真机记录基线。

## 验证命令及结果（最终门禁）
- `npm run typecheck` ✅
- `npm run test:unit` ✅ 39 文件 / 217 测试
- `npm run build` ✅ 1925 模块
- `cargo check --manifest-path src-tauri/Cargo.toml` ✅
- `cargo test --manifest-path src-tauri/Cargo.toml` ✅ 64 测试（54 单元 + 10 集成）
- `cargo build --release` ✅（编译级）

## 已知风险
- 运行时视觉验收（滚动 30s、WebView2 控制台、窄窗口）需真机补做；埋点工具已就绪。
- 阶段 4 的「当前模型自动/模型下拉」等 §8.2 目标细节沿用绑定服务自身模型（无单独每功能模型覆盖字段），属产品取舍待确认。
- 设置页的 AI 行为与分面治理为「治理即时保存 + AI 行为随保存设置」混合语义（§9.5 允许二选一，全页统一为草稿保存），报告已声明。
- 工作区未提交改动量大，阶段提交时需逐文件区分前序遗留与本轮增量。

## 回滚方式
- UI 阶段：回退对应文件提交即可（各阶段文件清单见上）。
- 无数据迁移，无 user_version 变更，无需备份恢复流程。
- 已删文件（VideoHoverOverlay/mediaPreview）在 git 历史可恢复；如后续导入页需要视频 hover 预览，可从历史恢复并仅挂到 PendingList。

## 下一阶段
- 真机运行态验收（WebView2 控制台、滚动录屏、冷启动时间戳、Viewer 双击、设置窄窗口）。
- 按 §14.1 建议分支拆提交（codex/startup-observability、codex/remove-grid-hover-preview、codex/viewer-page-boundary、codex/ai-settings-ia、codex/facet-settings-unification 等）。

## 需求变更记录（§0.2，提交说明必须包含）
```text
旧设计：素材库允许图片 popover 和视频 hover overlay。
当前需求：素材库禁止所有大图/视频 hover 预览，双击后进入 Viewer。
差异原因：用户在 2026-08-27 明确提出需求变更。
处理：删除素材库预览入口，Viewer 成为唯一大图/视频预览场景。
```