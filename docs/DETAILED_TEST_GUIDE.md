# 茶包素材（BagerTea AiMedias）详尽测试指导书

> **版本**：v1.1（2026-09-08）
> **v1.1 变更**：新增第 13 章「模块 K：代码规范静态审查」（K-0 / K-01~K-10）；新增补丁用例 A-17、F-13、F-14、I-11；2.4 基线四关 → 五关（补 `npm run build`，对齐 DEVELOPMENT.md 三关交付线）；附录 B 消灭 S5~S10 悬空项；执行节奏 Day 6 拆分为 Day 6~7；测试报告产出增加「代码健康度」小节
> **适用版本**：v1.0.1 Phase 3 · W5-W7 已完成（wp7-release tag 之后）
> **文档定位**：真机验收级测试指导书，直接对应 `docs/PROGRESS.md` 中"真机走查验收矩阵"任务（GUI 环境依赖项：播放器 / 收藏评级交互 / 备份恢复弹窗 / 相似图去重）
> **执行方式**：`npm run tauri dev` 启动应用，全程真机手工走查
> **预期结果依据**：本文预期结果基于 `docs/review/` 契约文档与 `docs/prd_bagertea_v2.md`；若与当前实现有出入，以契约为准并记录缺陷

---

## 0. 与既有测试体系的关系

| 文档 | 定位 | 与本文关系 |
|---|---|---|
| `docs/TEST_STRATEGY.md` | 自动化/技术测试策略 | 本文开测前先跑其基线（见 2.4 五关），自动化绿了才值得人肉测 |
| `docs/USER_TEST_GUIDE.md` | 2026-08-22 版用户走查（3 画像 / 8 旅程 / X-01~X-16 手滑场景） | 该文档早于 8 月底~9 月初的大改造（三区超级搜索、标签分面、收藏评级、感知去重、备份恢复），本文已覆盖其范围并大幅扩展；X-01~X-16 手滑场景仍可作回归参考 |
| **本文** | 真机验收级详尽测试指导书 | 覆盖 W5-W7 全部新功能 + 模块联动矩阵 + 三类核心问题专项 + 探索性测试章程 |

---

## 1. 总则

### 1.1 测试目标

本文要回答的不是"代码能不能跑、有没有 bug"，而是一个问题：

**站在一个第一次拿到这个软件的真实用户角度——这个软件，我敢不敢用它管我几万张吃饭的素材库？**

具体拆为三问（即老板定的三类核心问题，本文所有用例均标注类别）：

### 1.2 三类核心问题定义与判定口径

**A 类：UI 界面是否存在重复或冗余**
- 同一动作存在多个入口，但行为、文案、二次确认不一致（网格删除按钮 vs 右键删除 vs 快捷键）
- 同一信息重复展示（清单页与确认页重复展示相同统计）
- 完成一个用户意图所需操作超过必要步骤（给一张图打一个标签超过 3 次点击）
- 术语不统一：素材/文件/图片混用、标签/分面混用、"删除/移除"混用
- 视觉噪声：无信息量的图标、分隔线、装饰元素、过度留白

**B 类：各功能逻辑是否存在冲突或隐患**
- 跨模块状态不同步（AI 采纳了标签，但标签树计数没变）
- 边界条件错误（数值分面排序按字符串排：2 排在 10 后面）
- 并发/时序冲突（AI 打标批次进行中删除该素材；打标进行中恢复备份）
- 状态机泄漏（取消批次后状态仍停在 processing；建议拒绝后又冒出来）
- 规则冲突未被系统拦截（跨分面合并标签；同值条件同时进必须区与排除区）

**C 类：代码看似可行但实际不可用（最阴险，单测全绿但用户手上废掉）**
- 成功是假的：导出 move 完成提示成功，但库记录仍指向旧路径，查看器点开死链
- 失败被吞：删除文件失败（只读/被占用），但素材从列表里消失了（假删除），文件还在磁盘
- 功能存在但等于没有：入口存在但点击无响应；关键参数藏得太深没人找得到
- 长任务无反馈：1000 张导入中界面无任何进度变化，用户以为死机直接强杀
- 提示不可行动：报错只有裸 error code，用户不知道下一步该干嘛

### 1.3 方法论框架（四层，检索最佳实践后融合）

| 层 | 方法 | 用在哪 |
|---|---|---|
| 用例设计 | ISTQB 等价类划分 / 边界值分析 / 判定表 / 状态迁移 | 各模块用例表（每条注明用了什么技术） |
| 可用性检查 | Nielsen 十大可用性启发式（0-4 严重度评级） | 模块 B：UI 逐屏走查 |
| 场景验收 | UAT 用户旅程（导入→打标→搜索→导出主链路闭环） | 模块 L：联动矩阵 |
| 探索测试 | SBTM 章程驱动 + Tour 巡游 + PROOF 汇报格式 | 第 15 章探索性测试 |

### 1.4 缺陷严重度与用例优先级

**缺陷严重度**（与 USER_TEST_GUIDE 保持一致）：

| 级 | 定义 | 处理要求 |
|---|---|---|
| P0 | 崩溃 / 数据丢失 / 核心功能完全不可用 | 阻塞发布，当天修 |
| P1 | 主流程受阻，有绕行方案 | 发布前必修 |
| P2 | 次要功能缺陷、明显体验问题 | 排期修 |
| P3 | 文案、轻微 UI 瑕疵 | 记录即可 |

**用例优先级**：P0=必测且必须通过；P1=必测；P2=时间允许测；P3=顺带记录。

### 1.5 通过标准与质量评级

| 评级 | 结论 | 判定 |
|---|---|---|
| A | 可发布 | P0=0，P1≤3 且均有绕行方案，P0 级用例通过率 100% |
| B | 修复后可发布 | P0=0，P1 4~8 个 |
| C | 需再一轮测试 | 出现 P0，或 P1>8 |
| D | 阻塞 | P0≥3，或出现任何数据丢失类缺陷 |

### 1.6 执行纪律（硬约束）

1. **执行即记录**：每个用例当场标 ✅/⚠️/❌，禁止事后凭记忆补记
2. **先固定复现**：缺陷需连续复现 2 次才入库；复现不了的记"偶发"并留录屏
3. **截图/录屏命名**：`{模块}-{用例号}-{序号}.png`（如 `SS-C03-1.png`），与缺陷记录一一对应
4. **模块小结**：每完成一个模块花 5 分钟小结（缺陷数、有无拦路问题、是否继续）
5. **防误报**：不确定"是不是缺陷"时，先查 5.5 节负向范围与契约文档；仍不确定就记录并标"待判定"
6. 每个大轮（Day 1）开始前重置测试库，保证用例前置条件干净

---

## 2. 测试环境与素材准备

### 2.1 环境要求

| 项 | 要求 |
|---|---|
| 系统 | Windows 10/11（项目主力环境） |
| 分辨率 | 1920×1080 主测；1366×768 关键页复测（UI_DESIGN_SYSTEM 硬约束：两档分辨率均不得出现重叠） |
| DPI | 100% 主测；150% 抽查一轮布局 |
| AI 服务 | ① Ollama 已配置且可用；② 另备一组"坏配置"（错误端口/空地址）用于异常场景 |
| 启动 | `npm run tauri dev` |

### 2.2 测试库管理

- 每大轮开始前重置测试库：按 `docs/review/超级搜索三区改造与数值打标开发计划-2026-09-03.md` 中规定的"开工前重置测试库流程"执行
- 重置前，先用设置页的备份功能做一份**标准基线库备份**（打标完成、含收藏、回收站非空的状态），供模块 I（备份恢复）作为恢复源
- 测试期间改过的库，结束前不清理，留给回归轮使用

### 2.3 标准测试素材包（开工前一次性备齐）

| 包 | 内容 | 用途 |
|---|---|---|
| **S1 基础包**（约 30 文件） | 常规 JPG/PNG，中英文命名，部分带 GPS，拍摄日期跨年（验证 by_date 分目录） | 冒烟 + 日常用例 |
| **S2 全格式包** | JPG / PNG / WEBP / GIF（动+静）/ HEIC / MP4 / MOV（另补 HEVC 编码 MP4、AV1 编码 MP4 各一段，v1.1 增补·见 F-14）；>20MB 大图；>200MB 长视频；无 EXIF 的截图 | 格式覆盖（A-09、F-02、F-14、G-10） |
| **S3 压力包** | ≥1000 个混合文件（可复制扩充至 ≥10000，作为 A-17 万级库 soak 的种子，v1.1 增补） | 性能体感（A-12、E-09、G-11、F-10） |
| **S4 异常包** | 截断的 JPG（只下载一半）、0 字节文件、txt 伪装成 .jpg、只读属性文件、emoji 文件名、255 字符超长名、>10 层深目录 | 异常注入（A-07/A-08/A-10） |
| **S5 相似图包** | 同图不同尺寸×3、同图不同压缩×2、连拍×5、同场景不同瞬间×2、裁剪图、水平翻转图、完全相同副本×2（放不同目录） | 感知去重 + 同源文件组（J-04~J-08、A-15） |

### 2.4 基线自检（五关，全绿才开测）

与 DEVELOPMENT.md「三关交付线」（cargo test 全绿 + typecheck 零报错 + build 通过）对齐并扩充（v1.1 修正）：

```bash
cd src-tauri && cargo test     # 截至 2026-09-03 基线：573 passed
npx vitest run                 # 截至 2026-09-03 基线：545 passed
npm run typecheck
npm run build                  # build 通关：对齐 DEVELOPMENT.md 交付线（v1.1 增补）
npm run tauri dev              # 启动冒烟：主窗口正常渲染、无白屏
```

任一关红 → 停止人工测试，先修（自动化都过不去的点，人肉测试没有意义）。

**Day 0 延伸**：首次开测前按第 13 章 K-0 完成静态门禁摸底（clippy / fmt / unwrap 基线数），结果记入本轮测试报告附录，作为「代码规范不恶化」的对照线。

---

## 3. 模块 A：导入功能

**测试角度**：
1. 流程完整性——两段式入库（清单确认 → 改名模板构造器）每一步可理解、可回退、可取消
2. 异常韧性——坏文件不得连累好文件；失败必须可见且可读（体验断点六件套之"导入失败明细"）
3. 数据一致性——导入中途取消/强杀进程后，库自洽（无半条记录、无孤儿缩略图任务）
4. 体感——大批量导入时界面是"活着"的（短锁设计约束验证）

**前置条件**：全新测试库（按 2.2 重置）；S1~S5 素材包就绪。

| 编号 | 测试要点 | 验证方法（操作步骤） | 预期结果 | 类别 | 级 |
|---|---|---|---|---|---|
| A-01 | 空库首次引导 | 全新库首次启动，进入素材库页 | 空态不白屏，有明确引导入口（六件套#1），文案与实际导入入口一致，点击直达导入页 | C | P0 |
| A-02 | 文件夹递归导入 | 拖入含 3 层子目录的文件夹 | 清单包含全部子目录文件，路径展示正确，计数与磁盘实际一致 | F | P0 |
| A-03 | 单文件/多文件拖入 | 分别拖 1 个文件、拖 20 个散文件 | 均进入清单确认；散文件不丢、不重 | F | P0 |
| A-04 | 两段式流程完整性 | 走完 拖入→清单确认→改名模板→入库 全程；中途分别点返回/取消 | 每步可回退；取消后库无残留记录；确认页统计（数量/大小）与清单一致 | B | P0 |
| A-05 | 改名模板构造器 | 构造含 序号/日期/原文件名 的模板；构造会产生同名冲突的模板 | 预览实时更新；模板保证唯一文件名；纯中文模板名可用 | B | P1 |
| A-06 | 重复导入识别 | 同一文件夹导入两次；同一文件拖两次 | 第二次被识别为已入库（提示跳过/重复计数），不产生重复记录；同源文件正确归组而非重复 | B | P0 |
| A-07 | 损坏文件失败明细 | 导入 S4 中的截断 JPG 与 0 字节文件 | 失败明细页展示（六件套#2），失败原因是人话（非裸 error code/堆栈）；失败文件不入库；好文件正常入库不受连累 | C | P0 |
| A-08 | 伪装扩展名 | 导入 txt 改名的 .jpg | 识别为不可用并计入失败明细，不崩溃、不生成黑图缩略图 | B | P1 |
| A-09 | HEIC 解码 | 导入 S2 中的 HEIC 文件 | 缩略图正常生成（heif-bin 链路），查看器可打开 | F | P0 |
| A-10 | 中文/特殊字符/超长文件名 | 导入 S4：纯中文名、含空格/#/%/括号/emoji、255 字符长名 | 入库、缩略图、搜索、导出全链路无乱码无失败（边界值+特殊字符等价类） | B | P0 |
| A-11 | 导入中途取消 | 大文件夹导入进行中点取消 | 已完成部分状态一致保留；未完成部分无半条记录；可再次发起导入 | B | P1 |
| A-12 | 大批量导入体感 | 导入 S3（≥1000 文件），同时切到素材库页滚动浏览 | 进度持续更新；UI 不冻结；缩略图渐进出现（短锁验证——导入不阻塞浏览） | C | P0 |
| A-13 | 双层缩略图 | 导入完成后立即快速滚动网格 | 先占位图后高清异步加载，滚动不错位不闪烁（LRU 生效） | C | P1 |
| A-14 | 导入中断恢复 | 导入进行中直接强杀进程，重启应用 | 库自洽：无孤儿记录、无死缩略图任务、可继续导入 | B | P1 |
| A-15 | 同源文件组归组 | 导入 S5（同图不同尺寸、相同副本不同目录） | 按设计识别为同源文件组并归组展示（与模块 J 联动复验） | B | P1 |
| A-16 | 导入页 UI 一致性 | 走查清单列、排序、批量勾选、按钮文案 | 文案术语与其他页统一（"素材"vs"文件"不得混用）；勾选逻辑与其他页一致 | A | P2 |
| A-17 | 万级库 soak（规模对赌，v1.1 增补） | 将 S3 压力包复制扩充至 ≥10000 文件导入，随后浏览/搜索/查看器轮流操作一遍，应用不退出搁置过夜 | 次日验收：无崩溃、无私有内存/句柄持续膨胀（任务管理器对照启动值）、核心操作体感无明显劣化；直接对住第 1 章「几万张素材库」的风险命题；素材拷贝耗时大，可与 Day 1 其他用例并行 | B | P1 |

**重点盯防**：空库引导与导入失败明细是 2026-08-31 评测报告的体验断点修复项（六件套），本轮验证的是**落地质量**而非"存在即可"——引导是否直达、失败原因是否可读可行动。中断恢复（A-14）是历史薄弱区，必测。

---

## 4. 模块 B：UI 界面（含冗余专项）

**测试角度**：
1. 可用性底线——Nielsen 十大启发式逐屏过一遍
2. 冗余猎杀——专找重复入口、重复信息、多余步骤、术语不一致
3. 规范符合性——`docs/UI_DESIGN_SYSTEM.md` 硬约束逐条对

**走查范围**：五页面（ImportPage / LibraryPage / AiTaggingPage / SuperSearchPage / SettingsPage）+ 查看器 + 全部弹窗（导入确认、备份恢复、彻底删除确认、高级浮层、标签值 portal 浮层）。

### 4.1 Nielsen 十大启发式逐屏走查（U-01 ~ U-10）

| 编号 | 启发式 | 本项目具体检查点 | 验证方法 | 预期结果 | 级 |
|---|---|---|---|---|---|
| U-01 | 系统状态可见 | 导入进度、打标批次进度、缩略图加载、备份进度、搜索执行中，五处长任务 | 触发各长任务并观察 | 每处都有持续更新的进度反馈，进行中有明确"进行中"状态 | P0 |
| U-02 | 贴近真实世界 | 超级搜索人话预览、失败原因文案、分面命名 | 读一遍全部提示文案 | 用户不用猜：预览句无歧义（且/或/排除连接词清晰）；失败提示是中文人话+下一步建议 | P0 |
| U-03 | 用户可控 | 取消进行中任务、批次撤销、回收站恢复、弹窗可关 | 每个长任务尝试取消 | 全部可取消/可撤销/可恢复，无"点了就回不了头"的非危险操作 | P0 |
| U-04 | 一致性（重点） | 删除/打标/收藏/导出在 网格右键、工具栏、查看器、批量操作栏 多入口的行为 | 同一动作走遍所有入口对比 | 各入口行为、二次确认、结果完全一致（不一致即 A 类缺陷） | P0 |
| U-05 | 防错 | 彻底删除、恢复备份、清空回收站等危险操作 | 逐个触发 | 均有二次确认，且确认文案说清后果（"将物理删除文件，不可恢复"） | P0 |
| U-06 | 识别而非回忆 | 图标按钮 tooltip、条件卡回显已选值 | 鼠标悬停全部纯图标按钮；查看已建好的条件卡 | 纯图标必有 tooltip；条件卡非编辑态也显示已选值，不需要点开才能看 | P1 |
| U-07 | 灵活高效 | 快捷键（评级 1-5/0/F、删除、翻页）、批量操作、拖拽、条件持久化 | 用纯键盘完成 评级→收藏→删除 一套操作 | 快捷键可用且不与输入框冲突；高频操作有批量/快捷路径 | P1 |
| U-08 | 优美简约（冗余重点） | 每屏信息密度、重复信息块、装饰元素 | 逐屏截图后标注"删掉也不影响任务的元素" | 无重复统计块、无无信息量图标/分隔线；主 CTA 视觉唯一 | P1 |
| U-09 | 容错 | 全部错误路径：坏文件、无配置、路径不存在、AI 断连 | 逐个触发错误场景 | 错误文案 = 发生了什么 + 对用户的影响 + 下一步怎么办（三要素缺一即 C 类） | P0 |
| U-10 | 帮助与文档 | 空态引导、设置项说明 | 首次使用视角走查空态 | 每个空态告诉用户"这是什么 + 怎么开始" | P2 |

每条按 Nielsen 严重度 0-4 打分：0=不是问题，4=可用性灾难（阻碍任务完成）。3~4 分记 P1 缺陷，2 分记 P2。

### 4.2 UI 冗余与一致性专项检查（R-01 ~ R-10）

| 编号 | 检查项 | 验证方法 | 预期结果 | 级 |
|---|---|---|---|---|
| R-01 | 同功能多入口一致性 | 列出 删除/打标/收藏/导出/外部打开 的全部入口，逐对对比行为 | 行为一致；若刻意保留多入口，各入口语义有明确分工且不重复出现在同一屏 | P0 |
| R-02 | 重复信息 | 对比导入清单页与确认页、网格工具栏与批量操作栏的统计/按钮 | 同一信息不在相邻两步重复完整展示；同一动作按钮不在同屏出现两次 | P1 |
| R-03 | 操作步数计量 | 计数：①给一张图打一个标签 ②给 20 张图打同一标签 ③导出选中素材 ④从零搜到目标图并打开 | 分别 ≤3 / ≤4 / ≤3 / ≤5 次点击为达标；超标路径记 A 类缺陷并给精简建议 | P1 |
| R-04 | 术语一致性 | 全 app 文案走查：素材/文件/图片、标签/分面、删除/移除、库/相册 | 同一概念全 app 统一叫法（以 PRD 术语表为准） | P1 |
| R-05 | 视觉噪声 | 逐屏找：无信息量图标、多余分隔线、纯装饰元素、过度留白 | 找到的每一项标注"建议删除/合并"，计入 A 类 | P2 |
| R-06 | 相似控件交互统一 | 对比各处下拉选择、浮层、弹窗的打开/关闭/多选交互 | 交互模式全局统一（同为浮层不一半浮层一半弹窗） | P1 |
| R-07 | 死入口排查 | 点击全部可见按钮/链接（含设置页每个入口） | 无点击无响应的按钮；无跳转到空态且无引导的死链接 | P0 |
| R-08 | 设计规范符合性 | 截图+量具核对：侧栏宽（素材库 224px/打标栏 252px）、字号（根 15/正文 13/辅助 11-12px）、间距（区块 16/内部 12/行高 32-36px）、主 CTA 黑色实心、黑白灰主题 | 与 UI_DESIGN_SYSTEM.md 逐条一致 | P1 |
| R-09 | 主题一致性 | 走查状态色/danger 色使用场景 | 状态色仅用于状态语义（成功/警告/危险），不做装饰用 | P2 |
| R-10 | 窗口缩放适应性 | 1366×768 / 1920×1080 / 拖拽任意中间宽度 / DPI 150% 分别走查五页面+查看器 | 无重叠、无截断、无横向滚动条、条件卡一行不破行 | P0 |

---

## 5. 模块 C：超级搜索功能

**测试角度**：
1. 三区语义正确性——必须（硬过滤）/ 优先（软排序）/ 排除（硬否定）三区职责不得互相渗透（minimumShouldMatch 恒 0 是核心契约）
2. AI 智能搜可靠性——SearchIntentV2 解析正确、三层降级（strict/lenient/keyword）永不红字报错
3. 用户可理解性——一条条件一行、实时人话预览、零结果诊断
4. 状态管理——条件持久化、与普通库筛选隔离

**前置条件**：用标准基线库（素材已打标，含分面标签、数值标签、GPS、收藏评级），保证每个条件维度都有"命中样本"和"不命中样本"。

### 5.1 三区条件卡用例

| 编号 | 测试要点 | 验证方法 | 预期结果 | 类别 | 级 |
|---|---|---|---|---|---|
| C-01 | 三区职责纯粹性 | ①只在必须区加条件→命中集缩小；②只在优先区加条件→**结果数不变，仅排序变**；③只在排除区加条件→命中集缩小 | 三区各自只做该做的事；优先区任何操作不得改变结果总数（minimumShouldMatch=0 契约验证） | B | P0 |
| C-02 | 优先区拖拽排序 | 拖动优先区条件改变顺序 | 仅排序变化；拖拽手感正常；1366 宽度下拖拽仍可用 | B | P1 |
| C-03 | 排除区两态语义 | 切换"命中任一"/"全部命中"两态，对比结果集；构造语义易混的双重否定场景 | 两态结果集变化方向正确（任一排除更激进）；must_not 内部实现不产生双重否定翻转（2026-09-03 计划中 P0 修复项回归） | B | P0 |
| C-04 | 一条条件一行硬规则 | 选用超长标签值/长文件名构造条件 | 条件严格一行：不换行、不撑爆卡片，溢出用省略+悬浮完整展示（§6.1 硬规则） | A | P1 |
| C-05 | 高级浮层 | 打开"高级"浮层修改机器参数 | 浮层遮罩正确、不破行；默认收起；修改后主界面有相应变化 | A | P2 |
| C-06 | 实时人话预览 | 构造 5 组复杂条件（嵌套+排除+数值），逐组人工核对预览句 | 预览句与实际查询语义完全一致，连接词无歧义 | B | P0 |
| C-07 | 嵌套条件组上限 | 在必须区连续建组到第 4 层；切换单组 AND/OR | 第 4 层被拒绝或按钮不可再点（上限 3 层）；AND/OR 切换结果正确 | B | P1 |
| C-08 | facet_has_any | 对多值标签分面选 2 个值 | 任一命中即返回（或语义） | F | P1 |
| C-09 | facet_missing | 选"缺失该分面标签"条件 | 无该分面任何值的素材命中 | F | P1 |
| C-10 | 数值范围边界 | 数值分面条件取边界值（等于下限/等于上限/恰好超出） | 边界值命中符合操作符定义（开闭区间明确，边界值分析 3-value 法） | B | P1 |
| C-11 | 零结果诊断 | 构造必无结果的条件（排除掉所有） | 零结果页给出诊断：标红肇事条件/放宽建议，而非一片空白（§6.5） | C | P0 |
| C-12 | 条件持久化 | 建好条件→切到素材库→回来；重启应用再进来 | 条件完整恢复（六件套#6）；"清除全部"按钮可用且清得干净 | B | P1 |
| C-13 | 与普通库状态隔离 | 超级搜索设条件→返回素材库普通筛选 | 普通库筛选不受污染；回超级搜索条件仍在；两边结果互不串 | B | P0 |
| C-14 | 必须区×排除区同值冲突 | 同一标签同时加入必须区与排除区 | 系统行为有定义：结果为零或提示条件冲突；不得崩溃、不得静默返回全库 | B | P1 |
| C-15 | 只加优先区（无硬条件） | 不加任何必须条件，只加优先条件 | 结果数=全库，按偏好排序；不得出现空结果或异常 | B | P0 |
| C-16 | 空条件执行 | 不加任何条件直接执行 | 等价全库浏览，行为有明确定义（非报错） | B | P2 |

### 5.2 AI 智能搜与降级用例

| 编号 | 测试要点 | 验证方法 | 预期结果 | 类别 | 级 |
|---|---|---|---|---|---|
| C-17 | strict 精确解析 | 输入可精确映射的自然语言（如"找夜景且有人物的照片"） | SearchIntentV2 正确生成条件卡；生成结果与人话预览一致；结果集正确 | F | P0 |
| C-18 | lenient 部分剔除 | 输入部分概念无法映射的语句 | 剔除不可执行部分继续执行，UI 明示哪些被忽略及原因（部分剔除规则） | B | P0 |
| C-19 | keyword 兜底 | 输入完全无法结构化的语句 | 降级为全文关键词检索；**永不红字报错**（三层降级契约） | B | P0 |
| C-20 | AI 服务不可用 | 用坏配置（错误端口）发起智能搜 | 降级为关键词检索或给出可行动提示（含跳转设置）；不挂死、不裸报连接错误 | C | P0 |
| C-21 | 输入边界 | 空输入、超长输入、含引号/括号/emoji 的输入 | 安全处理：空输入不执行、特殊字符不破坏解析 | B | P1 |
| C-22 | 结果稳定性 | 命中大量结果时连续滚动翻页 | 虚拟滚动顺序稳定：不重复、不丢失、不错位 | B | P1 |

### 5.3 负向范围（防误报，执行前必读）

加载 `docs/review/超级搜索条件组前端UI改造方案-2026-09-06.md` **§12"明确不做清单"（9 条）**，逐条确认当前 UI 不会让用户误以为支持——不支持的能力应当"无入口"或灰置+说明，而不是"有入口但点了没反应"（后者本身就是 C 类缺陷）。

---

## 6. 模块 D：标签系统功能

**测试角度**：
1. 分面协议正确性——tag_facets 唯一事实源、resolve_facet_key 三分支路由、input_mode 两模式
2. 数据完整性——合并/删除/治理操作的级联规则与计数同步
3. AI 与手动边界——manual_only 分面 AI 不得写入；建议与手动操作的状态机

**前置条件**：标准基线库；已了解 10 个系统分面清单（以 `docs/review/facet-contract-v2.md` 为准）。

| 编号 | 测试要点 | 验证方法 | 预期结果 | 类别 | 级 |
|---|---|---|---|---|---|
| D-01 | 系统分面完整性 | 打开分面管理面板核对 10 个系统分面 | key/display_name 与契约一致；系统分面不可删除（入口灰置或无入口） | F | P0 |
| D-02 | 自定义分面创建 | 新建自定义分面；把 max_items 设为 2 后打满 | 创建成功可选 input_mode 与 max_items；打满上限后再加标签被明确拒绝并提示（静默失败=缺陷） | B | P1 |
| D-03 | input_mode=manual_only | 对 manual_only 分面：手动打标成功后，触发 AI 打标 | AI 不写入该分面（打标完成后该分面无 AI 新值）；手动写入不受影响 | B | P0 |
| D-04 | input_mode=ai_and_manual | 对 ai_and_manual 分面分别手动、AI 打标 | 两者均可写入；数据不冲突不重复 | B | P1 |
| D-05 | resolve_facet_key 三分支 | 按 facet-contract-v2.md 的三分支用例复演（精确匹配/别名/未知 key） | 各走对分支，路由结果与契约一致 | B | P1 |
| D-06 | 合并标签（同分面） | 合并同分面两个标签（各挂若干素材） | 素材计数迁移正确；标签树只剩合并后标签；网格展示同步 | F | P0 |
| D-07 | 合并标签（跨分面） | 尝试合并两个不同分面的标签 | 明确拒绝并说明"只限同分面"；不得产生悬空数据 | B | P0 |
| D-08 | 分面级联删除 | 删除一个自定义分面（其下有标签、有素材关联） | 按契约级联顺序执行（先关联、再标签、后分面）；**素材本体绝不删除**；二次确认；标签树/搜索条件/已选筛选同步清空 | B | P0 |
| D-09 | 标签树准确性 | 打标前后看标签树计数；长标签名；深层展开 | 计数实时准确；长名省略+悬浮完整；展开折叠状态正常 | F | P1 |
| D-10 | 词汇治理面板 | 重命名一个标签；禁用一个标签 | 全局生效：网格、超级搜索条件卡、导出 by_tag 目录名同步更新 | B | P1 |
| D-11 | 单图手动打标 | 在查看器与网格各给单图打一个分面标签 | 立即生效；计数 +1；撤销入口可用 | F | P0 |
| D-12 | 批量打标与撤销 | 多选 20 张批量加分面标签，然后执行批次撤销 | 全部生效；撤销后全部恢复原状（tag_ops 流水验证，含中间又有其他打标操作时撤销范围的正确性） | F | P0 |
| D-13 | 数值打标输入校验 | 数值分面输入：合法数、非法文本、负数、超大数、小数位 | 非法输入被拒绝且提示；合法值写入后显示格式一致 | B | P1 |
| D-14 | 标签与搜索即时联动 | 新建标签后立即去超级搜索条件卡找它；删除标签后看已建条件 | 新标签立即可选；被删标签的条件引用被清空或标失效提示，不产生死引用 | B | P0 |
| D-15 | AI 建议与手动冲突 | 建议 pending 状态下手动打同一标签 | 状态机转换正确（pending→confirmed），不产生重复标签实例 | B | P1 |

---

## 7. 模块 E：AI 打标

**测试角度**：
1. 闭环完整性——一键送打标→批次→建议→采纳/拒绝 全链路
2. 异常韧性——无配置、服务中断、批次中文件被删/损坏
3. 状态机严格性——pending→processing→done/cancelled 与建议 pending→confirmed/rejected 无泄漏

| 编号 | 测试要点 | 验证方法 | 预期结果 | 类别 | 级 |
|---|---|---|---|---|---|
| E-01 | 一键送打标 | 从库/筛选结果一键送一批素材打标 | 批次创建，状态 pending→processing，进度可见 | F | P0 |
| E-02 | 无配置跳转 | 清空 Ollama 配置后点打标 | 跳转设置页并聚焦 AI 连接管理（六件套#3）；不裸报错、不静默无反应 | C | P0 |
| E-03 | 建议采纳 | 打标完成后在待确认区确认建议 | pending→confirmed，写入对应分面；标签树计数更新 | F | P0 |
| E-04 | 建议拒绝（设计约束） | 拒绝建议 | pending→rejected；**数据不物理删除**（设计约束）；状态持久（重启后不再冒出） | B | P0 |
| E-05 | 批次中途取消 | processing 中点取消 | 状态转 cancelled；已完成部分结果保留且一致；未处理素材回到可再送状态 | B | P1 |
| E-06 | 批次熔断验证（S3 基线） | 构造中途失败：送打标后中途断开 Ollama | 观察批次行为：是否熔断/继续/卡死；与 2026-08-31 报告 S3（批次无熔断）基线对比，未修复则记录现状 | B | P1 |
| E-07 | 打标中删除素材 | 批次进行中删除其中一张素材 | 跳过或标记该素材，批次不崩溃不中断错乱 | B | P1 |
| E-08 | 打标中导入并发 | 批次进行中导入新文件 | 新文件不混入当前批次（或明确排队）；两任务结果互不污染 | B | P1 |
| E-09 | 大批量体感 | 送 300+ 张打标 | 进度持续反馈、可取消、UI 不冻结；预计剩余时间或处理速度可见（短锁验证） | C | P1 |
| E-10 | 打标中断恢复 | processing 中强杀进程重启 | 批次状态一致：孤儿批次有明确状态（可重试/可取消），不永久卡 processing | B | P2 |
| E-11 | 建议质量抽查（P1-P10 基线） | 对同一批素材抽查 20 条建议的准确性 | 记录错标率；与 2026-08-31 报告 P1-P10（本地模型提示词问题）基线对比是否改善 | B | P2 |

---

## 8. 模块 F：查看器

**测试角度**：
1. 播放/浏览基础能力（PROGRESS.md 点名的 GUI 依赖项之一）
2. 键盘操作流（评级快捷键是 W6 新交互，重点）
3. 异常素材容错

| 编号 | 测试要点 | 验证方法 | 预期结果 | 类别 | 级 |
|---|---|---|---|---|---|
| F-01 | 大图缩放平移 | 打开 >20MB 大图，放大到极限、平移到边角 | 打开速度可接受；缩放流畅；平移边界不飞出、不白屏 | F | P0 |
| F-02 | 视频播放 | 播放 MP4/MOV：播放/暂停/进度拖拽/音量/全屏 | 全部可用；时间轴与实际时长一致 | F | P0 |
| F-03 | 视频边界 | <1s 超短视频、>200MB 长视频 | 时间轴不异常；seek 准确；长视频不卡死 | B | P2 |
| F-04 | 键盘导航 | ←/→ 切换、Esc 关闭、空格暂停（视频） | 响应一致；连按不丢帧不错序 | F | P1 |
| F-05 | 收藏评级快捷键 | 查看器中按 1-5 设星级、0 清除、F 切换收藏（以实际快捷键提示为准） | 星标即时刷新；网格/列表同步更新；与鼠标操作结果一致 | F | P0 |
| F-06 | GPS 展示 | 打开含 GPS 与不含 GPS 的素材各一张 | 含 GPS 展示位置信息（六件套#5）；不含 GPS 不显示空块/占位噪声 | F | P1 |
| F-07 | 右键外部打开 | 右键"在文件管理器中显示/外部打开" | 打开路径正确且选中该文件；move 导出后的素材再点此项的行为有明确处理（不死链） | C | P1 |
| F-08 | 同源组切换 | 在同源文件组内切换浏览 | 组内切换流畅；组关系标识清晰 | B | P2 |
| F-09 | 查看器内打标 | ViewerTagBar 加/删标签 | 即时生效；退出查看器后网格与计数一致 | F | P1 |
| F-10 | 胶片条定位 | 大库中打开查看器，滚动胶片条后点击某项 | 当前项高亮与主画面一致；滚动定位不错位（压力包验证） | B | P2 |
| F-11 | 损坏文件打开 | 打开 S4 中的损坏文件 | 友好错误占位提示，不白屏不崩溃 | B | P1 |
| F-12 | 返回状态保持 | 滚动到网格中部→进查看器翻几张→退出 | 滚动位置与选中态保持，不跳回顶部（高频体验痛点） | C | P1 |
| F-13 | 性能基线考核线（v1.1 增补） | 秒表+任务管理器实测：冷启动至主界面可交互、千级库首屏渲染、查看器连续翻页 | 冷启动 <3s（含 splash）；千级库首屏 <2s；翻页无可感知延迟；超线记 C 类缺陷并附实测值（PERFORMANCE.md 的并发/性能专项不重复，本条只做「够不够及格」的考核线） | C | P1 |
| F-14 | HEVC/AV1 编码容错（v1.1 增补） | 播放 S2 补充的 HEVC 编码 MP4 与 AV1 编码 MP4 各一段 | 系统装有 HEVC 解码扩展时正常播放；缺解码器时给出可行动提示（如引导安装系统 HEVC 视频扩展），绝不黑屏/卡死/静默无声——WebView2 依赖系统解码器，这是真实环境必踩的雷 | B | P1 |

---

## 9. 模块 G：导出功能

**测试角度**：
1. 三布局正确性——flat / by_tag / by_date 目录结构与文件完整
2. copy 与 move 的差异——**move 后库记录同步是"看似可行实际不可用"的头号嫌疑点**
3. 失败诚实性——路径异常/同名冲突/磁盘问题不得假成功

| 编号 | 测试要点 | 验证方法 | 预期结果 | 类别 | 级 |
|---|---|---|---|---|---|
| G-01 | flat 布局 | 导出选中素材为平铺结构 | 目录下文件齐全、可打开、无损 | F | P0 |
| G-02 | by_tag 布局 | 导出带多标签素材 | 按标签建子目录；多标签素材归属策略与设计一致且行为可预期 | F | P0 |
| G-03 | by_date 布局 | 导出拍摄日期跨年的素材 | 按日期分目录；日期口径（拍摄/导入）与设计一致且文档化 | F | P0 |
| G-04 | CSV 导出 | 导出 CSV 后用 Excel 直接双击打开 | UTF-8 BOM 生效：中文无乱码；字段完整（路径/标签/日期等）；行数与选集一致 | F | P0 |
| G-05 | copy 模式 | copy 导出后核对源目录与库 | 原文件保留；库记录不变；缩略图正常 | F | P0 |
| G-06 | move 模式同步（头号盯防） | move 导出后：回库看该素材 | 库记录与文件系统一致（记录按设计移除或标记）；缩略图不出现死链；超级搜索不再命中已移走素材；查看器不出现打不开的死入口 | C | P0 |
| G-07 | 同名冲突 | 导出来自不同目录的同名文件 | 自动重命名或明确提示冲突；**绝不静默覆盖** | B | P0 |
| G-08 | 目标路径异常 | 导出到：不存在路径/只读目录/（模拟）磁盘满 | 明确失败+失败明细（哪些成功哪些失败）；不假成功 | C | P0 |
| G-09 | 空选集导出 | 未选中任何素材点导出 | 按钮置灰或明确提示，不进入导出流程 | B | P1 |
| G-10 | 导出含视频 | 选集含 MP4/MOV 导出 | 文件完整可播放 | F | P1 |
| G-11 | 大批量导出体感 | 导出 300+ 文件 | 进度反馈、可取消；中途取消后目标目录状态有说明（半成品不伪装成完成）；磁盘满/写失败场景以 G-08 为准考核，此处不重复计用例（v1.1 备注） | C | P1 |
| G-12 | 从超级搜索结果导出 | 超级搜索命中集直接导出 | 只导出当前命中集；被排除区排除的素材绝不在内（联动 L-04） | B | P0 |

---

## 10. 模块 H：删除与回收站

**测试角度**：
1. 软删语义——普通删除不物理删文件（设计约束）
2. 恢复完整性——恢复后缩略图/标签/收藏/数值全量回来
3. 假删除猎杀——删除失败时记录必须保留（C 类重点）

| 编号 | 测试要点 | 验证方法 | 预期结果 | 类别 | 级 |
|---|---|---|---|---|---|
| H-01 | 单素材软删 | 删除一张图 | 从网格消失、计数-1；**磁盘文件仍在**（软删设计约束）；进回收站 | F | P0 |
| H-02 | 批量删除 | 多选删除 50 张 | 全部入回收站；磁盘文件全部保留 | F | P0 |
| H-03 | 恢复完整性 | 恢复一张带标签+星级+数值标签的素材 | 回到原位置（或库根，以设计为准）；缩略图正常；标签、星级、数值全量恢复 | F | P0 |
| H-04 | 彻底删除 | 回收站中彻底删除 | 二次确认（说清不可恢复）；此时才物理删除文件 | B | P0 |
| H-05 | 假删除猎杀 | 对只读文件/正在播放占用中的视频执行删除 | 删除失败时**记录必须保留在列表**，且有失败提示；"列表里没了但文件还在"= P0 缺陷 | C | P0 |
| H-06 | 回收站内操作 | 回收站内浏览/排序/搜索/选择性恢复 | 全部可用；恢复目标正确 | F | P1 |
| H-07 | 回收站与搜索隔离 | 删除素材后执行超级搜索 | 回收站素材不出现在结果中 | B | P0 |
| H-08 | 删除后计数同步 | 删除带标签素材 | 标签树计数同步 -1 | B | P1 |
| H-09 | 清空回收站 | 一键清空 | 二次确认+后果说明；物理删除生效；计数清零 | B | P1 |
| H-10 | 删除与打标并发 | 素材在打标批次中（见 E-07，此处复验回收站侧） | 该素材入回收站后批次结果与回收站状态一致 | B | P1 |

---

## 11. 模块 I：设置与数据管理

**测试角度**：
1. 备份恢复闭环（W6 新功能，PROGRESS.md 点名 GUI 依赖项）——原子性是生命线
2. 配置可靠性——双源不同步是已知历史问题（S4 基线）
3. 危险操作防错

| 编号 | 测试要点 | 验证方法 | 预期结果 | 类别 | 级 |
|---|---|---|---|---|---|
| I-01 | AI 连接配置 | 配置正确地址→测试连接；再配错误地址 | 成功有明确反馈；失败提示可读（含排查建议），不裸报 error | F | P0 |
| I-02 | 配置双源核对（S4 基线） | 同一配置改完后：设置页显示值 vs 实际生效值（发起打标验证用的哪个） | 两处一致；不一致= S4 未修复，记录 | B | P1 |
| I-03 | 创建备份 | 设置页发起数据库备份 | 备份文件生成、路径可选、有进度反馈、完成明确 | F | P0 |
| I-04 | 恢复备份 | 用标准基线备份恢复 | 恢复前二次确认（覆盖警告）；恢复后数据完整：素材记录/标签/收藏评级/回收站/数值标签逐项核对 | F | P0 |
| I-05 | 恢复中断原子性 | 恢复进行中取消/模拟失败 | **要么旧库要么新库，绝无半库状态**（原子性验证——出现中间态即 P0） | B | P0 |
| I-06 | 备份内容对账 | 恢复后抽样对账：素材数/标签数/星级数/回收站数 vs 备份时记录 | 数字逐项一致 | B | P1 |
| I-07 | 设置持久化 | 逐项修改设置→重启 | 全部保留 | F | P1 |
| I-08 | 日志完整性（S1 基线） | 正常操作+一次崩溃后查日志文件 | 日志落盘完整、可按天定位；崩溃场景日志不丢（S1 未修复则记录） | B | P2 |
| I-09 | 迁移幂等（S2 基线） | 如有旧版本库：升级后再次启动 | 迁移不重复执行（S2 未修复则记录） | B | P2 |
| I-10 | 危险操作防错总检 | 走查全部危险按钮：恢复备份/清空回收站/彻底删除/删除分面 | 均有二次确认且文案说清后果；确认按钮不是默认焦点（防回车误触） | A | P1 |
| I-11 | 备份与日志隐私（v1.1 增补） | 打开备份文件与应用日志查看内容：备份是否明文可读、日志是否含用户完整磁盘路径/文件名 | P3 级观察项：明文备份与路径暴露只做记录不卡发布，但须写入报告「遗留风险」一节（素材库属个人数据，隐私面不可裸奔） | C | P3 |

---

## 12. 模块 J：W5-W7 新功能专项

**测试角度**：新功能本体正确性 + 与既有功能的意外交互（新功能最容易和老功能打架）。

### 12.1 收藏评级

| 编号 | 测试要点 | 验证方法 | 预期结果 | 类别 | 级 |
|---|---|---|---|---|---|
| J-01 | 评级数据链路 | 评级→网格显示星标→按评级筛选→按评级排序 | 全链路正确；筛选与排序用数值序（见 J-10） | F | P0 |
| J-02 | 快捷键与输入框冲突 | 焦点在搜索框/重命名框时按 1-5/0/F | **不得触发评级**（快捷键仅在素材焦点态生效） | B | P0 |
| J-03 | 批量评级 | 多选 20 张批量设星级、批量取消 | 全部生效；与单张操作结果一致 | F | P1 |

### 12.2 感知去重（dHash）

| 编号 | 测试要点 | 验证方法 | 预期结果 | 类别 | 级 |
|---|---|---|---|---|---|
| J-04 | 相似图识别 | 用 S5 包：同图不同尺寸/压缩/完全相同副本 | 正确识别为相似组并归组展示 | F | P0 |
| J-05 | 误报检查 | 用 S5 包：同场景不同瞬间×2、裁剪图、翻转图 | 内容相似但确实不同的图不误报（或误报率记录在案）；阈值表现记录 | B | P1 |
| J-06 | 去重决策流 | 去重视图中"保留一张删其余" | 删除部分进回收站（软删，可恢复）；保留张不动 | B | P0 |
| J-07 | dHash 边界表现 | 裁剪/翻转/黑白滤镜图的归组行为记录 | 无论归不归组，**行为与文档说明一致**即通过；重点是"说不清"的状态 | B | P2 |

### 12.3 同源文件组与数值分面

| 编号 | 测试要点 | 验证方法 | 预期结果 | 类别 | 级 |
|---|---|---|---|---|---|
| J-08 | 同源组操作 | 组内切换浏览；整组删除；整组导出 | 组操作作用于全组且结果可预期；单张操作不误伤同组其他张 | B | P1 |
| J-09 | 数值分面全链路 | 建数值分面→数值打标→范围搜索→数值排序 | 全链路数值语义（详见 D-13、C-10） | B | P1 |
| J-10 | 数值排序陷阱 | 打值 2、10、100 三档后按该分面排序 | 数值序 2<10<100；**字符串序（10 排在 2 前面）= P0 缺陷** | B | P0 |
| J-11 | 数值与文本混用 | 数值分面输入文本 | 校验拒绝；已有文本值的历史数据处理有定义 | B | P1 |

---

## 13. 模块 K：代码规范静态审查（v1.1 新增）

**测试角度**：
1. 让「违反 DEVELOPMENT.md 一律打回」从口号变为可执行流程——项目当前无任何 lint/静态门禁（无 eslint/prettier/clippy/rustfmt 配置），代码规范事实上只靠人肉 review，本章把把关下沉为命令
2. 铁律行为化——分层铁律、性能红线、Rust 风格逐条给命令与判定线；黑盒走查（模块 A~L）天然覆盖不到代码层，由本章静态兜底
3. 债务可量化——所有指标先记现状基线，「不恶化」是硬线、「持续下降」是目标

**执行方式**：不需要启动应用。仓库根目录 + Git Bash 即可执行（Windows 下先 `export PATH="/c/Users/33887/.cargo/bin:$PATH"` 引入 cargo）。

### 13.1 K-0：Day 0 静态门禁补建（一次性，约半天，可与 Day 1 上午合流）

| 步骤 | 动作 | 判定与产出 |
|---|---|---|
| K-0a | `cd src-tauri && cargo clippy -- -D warnings` 摸底 | 记录现有告警数为「clippy 基线数」；存量告警允许豁免（逐条加 `#[allow]` 并注明豁免原因），新增代码告警零容忍 |
| K-0b | `cd src-tauri && cargo fmt --check` 摸底 | 记录基线差异数；建议 Day 0 当天全量 `cargo fmt` 一次，此后以全绿为常态 |
| K-0c | 前端最小 eslint（可选） | 若本轮暂不引入 eslint，则仅执行 K-08 的 grep 级检查并记为遗留风险；若引入，推荐最小集：`eslint:recommended` + `react-hooks/rules-of-hooks` + `react-hooks/exhaustive-deps`（正是 2026-08-27 实锤的 hook 顺序类崩溃的预防项） |
| K-0d | 汇总现状基线数 | K-01~K-08 各记一个「现状数」，写进本轮测试报告附录，作为「不恶化」对照线 |

### 13.2 铁律自查用例（K-01 ~ K-08）

| 编号 | 检查项 | 命令与方法（仓库根目录 Git Bash） | 判定线 | 级 |
|---|---|---|---|---|
| K-01 | commands 薄壳（分层铁律 3.2） | `grep -rln "rusqlite\|SELECT \|INSERT \|UPDATE \|DELETE " src-tauri/src/commands/` | 零命中；command 只许校验/锁/事件，不得出现 SQL 关键字与业务逻辑 | P1 |
| K-02 | service 不拼 SQL（分层铁律 3.2） | `grep -rnE 'format!.*(SELECT|INSERT|UPDATE|DELETE)' src-tauri/src/services/` | 零命中；拼接式 SQL 属高危（注入风险），SQL 只准存在于 db/ 层 | P1 |
| K-03 | 图像解码唯一引擎（性能红线 3.3） | `grep -rln 'image::\|heif\|libwebp\|mozjpeg' src-tauri/src/ \| grep -v imaging` | 仅 imaging.rs 及其声明出现；另起解码引擎即缺陷 | P1 |
| K-04 | 不裸 unwrap（Rust 风格 四） | `grep -rn '\.unwrap()' src-tauri/src --include='*.rs' \| wc -l` | 不高于 K-0d 基线数；`#[cfg(test)]` 测试代码豁免 | P2 |
| K-05 | 锁内禁耗时（性能红线 3.3） | `grep -rn 'Mutex\|RwLock\|\.lock()\|\.write()' src-tauri/src/services/` 列候选点，人工逐处看锁持有区间是否含解码/网络/大文件 IO | 人工确认清单逐处判断，发现一处记一处；本项是铁律，命中即 P0。只给方法不给行号级清单，避免误报，执行者现场判断 | P0 |
| K-06 | 迁移纪律（Rust 风格 四） | `git log --oneline -- '*migrations*'` 核对历史迁移未被回改；核对 user_version 单调递增 | 已发布迁移不改动；版本递进无跳号/回退 | P0 |
| K-07 | 配置双源禁制（Rust 风格 四 + S4 基线） | `grep -rn 'settings' src-tauri/src --include='*.rs' \| grep -i 'read\|load\|get_'` 核对读取路径唯一；人工核对 normalize() 无写回 | 单一读取路径；normalize() 只读；与 I-02 行为级验证互为印证（I-02 红时优先怀疑本项） | P1 |
| K-08 | 前端职责分层（3.2） | 大文件榜：`find src -name '*.ts' -o -name '*.tsx' \| xargs wc -l \| sort -rn \| head -20`；page 直调：`grep -rn 'invoke(' src/pages/ \| wc -l` | page >300 行或 pages/ 直调 invoke >0 记观察项（请求细节应沉到 api/ 层）；不计缺陷但写入代码健康度 | P2 |

### 13.3 与本轮测试的联动规则（K-09 ~ K-10）

- **K-09 缺陷归属**：K 用例发现的问题走标准缺陷模板（类别 = F），但严重度按「对可维护性的长期风险」评估，不一刀切按 P0/P1 卡死发布——历史债务不该阻塞功能发布。K 结论在测试报告中单独成节：**代码健康度**（见第 17 章）。
- **K-10 修复把关铁律**：本轮任何缺陷修复，交付前必须过 2.4 五关与 K-0 门禁；禁止「修 A 破 B」的掉队式修复。

---

## 14. 模块联动与交互测试矩阵

**测试角度**：单模块全绿 ≠ 系统可用。本章专测"功能 A 的输出成为功能 B 的输入"时的状态流转——历史经验里，三类核心问题中的 B、C 类大多藏在这里。

### 14.1 联动场景用例（L-01 ~ L-15）

| 编号 | 联动链路 | 验证方法 | 预期结果 | 类别 | 级 |
|---|---|---|---|---|---|
| L-01 | **主链路闭环（终极 UAT）** | 导入 S1→AI 打标→采纳→超级搜索命中→查看器评级→导出 by_tag | 全程数据一致、无一步卡壳；这是产品的"一日三餐"路径，任何一步断裂都是 P0 | B/C | P0 |
| L-02 | 打标→搜索即时性 | 采纳新标签后，用**已保存的**搜索条件（含该标签）再查 | 立即命中（持久化条件对新数据生效） | B | P0 |
| L-03 | 删除→回收站→恢复→搜索 | 删除→恢复→用原标签搜索 | 恢复后可再次命中 | B | P0 |
| L-04 | 超级搜索→导出隔离 | 带排除区条件的结果集直接导出 | 导出文件与命中集严格一致 | B | P0 |
| L-05 | 批量撤销→搜索回退 | 撤销一批打标后重跑当时命中的搜索 | 结果数相应减少，无残留命中 | B | P1 |
| L-06 | 合并标签→条件卡/导出 | 合并标签后看已建搜索条件与 by_tag 导出 | 条件卡引用更新或清空（无死引用）；导出目录用新名 | B | P1 |
| L-07 | move 导出→查看器/外部打开 | move 导出后从历史记录/回收站相邻素材进入查看器 | 无死链入口；"外部打开"对已移走素材有明确处理 | C | P0 |
| L-08 | 备份恢复→进行中任务 | 打标批次进行中执行恢复备份 | 任务队列与库一致：无孤儿批次、无对已不存在素材的写入 | B | P1 |
| L-09 | 去重删除→回收站→恢复→重组 | 去重删掉的张恢复后 | 去重组重新归组正确 | B | P2 |
| L-10 | 评级→排序→虚拟滚动 | 按评级排序后快速滚动翻页 | 顺序稳定不跳变（与 C-22 交叉复验） | B | P1 |
| L-11 | 打标中→删除→回收站→批次完成 | E-07 + H-10 全链路复演 | 三方状态（批次/回收站/标签计数）最终一致 | B | P1 |
| L-12 | 词汇治理重命名→超级搜索 | 重命名标签后看条件卡已选值 | 已选值同步显示新名，执行结果不变 | B | P1 |
| L-13 | 空态联动 | 把库删空（全进回收站）后走五页面 | 各页空态一致、不误导、不报错；导入页正常工作 | A | P2 |
| L-14 | 回收站恢复→同源组/去重组 | 删除同源组一员→恢复 | 组关系正确重建 | B | P2 |
| L-15 | 恢复备份→收藏/回收站非空态 | 用"回收站非空+有收藏"的基线备份恢复 | 回收站内容与收藏评级完整恢复（不只恢复在库素材） | B | P1 |

### 14.2 模块 × 模块快速扫描矩阵

每个"操作"发生后，快速扫一遍受影响面（适合作为每轮回归的 15 分钟速查表）：

| 操作 \ 受影响面 | 素材网格 | 标签树计数 | 超级搜索 | 查看器 | 导出 | 回收站 |
|---|---|---|---|---|---|---|
| AI 采纳标签 | 卡片标签更新 | +1 | 立即可命中 | 标签栏更新 | by_tag 含新标签 | — |
| 手动批量打标 | 全部更新 | +n | 立即可命中 | 更新 | 含 | — |
| 批量撤销 | 恢复原状 | -n | 结果回退 | 恢复 | 不含 | — |
| 合并标签 | 展示新名 | 迁移正确 | 条件引用更新 | 更新 | 目录用新名 | — |
| 删除分面 | 关联清空 | 分面消失 | 条件清空/失效提示 | 标签栏清该项 | by_tag 无该分面 | — |
| 软删素材 | 消失 | -n | 不再命中 | 无死入口 | 不含 | 出现 |
| 恢复素材 | 回原位 | +n | 再次命中 | 完整可开 | 含 | 消失 |
| move 导出 | 记录移除/标记 | — | 不再命中 | 无死链 | 完成 | — |
| 评级/收藏 | 星标显示 | — | 可按评级筛选 | 快捷键生效 | — | — |
| 重命名标签（治理） | 更新 | 更新 | 条件卡同步 | 更新 | 新名 | — |

---

## 15. 探索性测试章程（SBTM）

**为什么需要**：用例表再全也只覆盖"想到的"；SBTM 用时间盒+章程驱动，专挖"没想到的"。

**章程模板**：Explore **[目标]** with **[资源]** to discover **[信息]**（45~90 分钟一个 timebox，中途不打断）

### 15.1 章程清单（6 条，按顺序执行）

| # | 章程 | 时长 |
|---|---|---|
| SBTM-1 | Explore 导入→导出全链路 **with** S2+S4+S5 大杂烩包 **to discover** 数据丢失、路径不同步、假成功 | 60min |
| SBTM-2 | Explore 超级搜索三区 **with** 疯狂条件组合（同类多条件、互相矛盾、AI 生成后手改） **to discover** 错误命中集、崩溃、状态污染 | 60min |
| SBTM-3 | Explore AI 打标批次 **with** Ollama 断连、慢响应、批次中途删文件 **to discover** 卡死、状态机泄漏、半成品数据 | 45min |
| SBTM-4 | 破坏者巡游（Saboteur Tour）：磁盘只读/接近满、断网、快速连点按钮、窗口极端缩放、DPI 150% 乱切 **to discover** 崩溃与不可恢复状态 | 45min |
| SBTM-5 | Explore 收藏评级+感知去重+同源组 **with** S5 相似图包 **to discover** 误删、误报、组关系错乱 | 45min |
| SBTM-6 | Explore 备份恢复 **with** 打标进行中/回收站非空/收藏存在的脏状态库 **to discover** 数据丢失与半库状态 | 45min |

### 15.2 每个章程结束后的 PROOF 汇报（5 分钟内完成）

- **P**ast：这段时间测了什么
- **R**esults：发现了什么（缺陷/疑问/值得深挖的线索）
- **O**bstacles：什么阻碍了测试（环境问题/阻塞缺陷）
- **O**utlook：还剩什么没测、下一步建议
- **F**eelings：直觉判断——"这个地方感觉不对劲但说不清"也要记，那是下轮章程的种子

---

## 16. 缺陷记录与执行管理

### 16.1 缺陷记录模板（每条必填）

```
缺陷ID：{模块}-{序号}（如 C-03-1）
发现日期 / 执行人：
关联用例：C-03
严重度：P0 / P1 / P2 / P3
问题类别：A（UI冗余）/ B（逻辑冲突）/ C（看似可行实际不可用）/ F（基础功能）
复现步骤：①…②…③…
预期结果：
实际结果：
复现概率：必现 / 高 / 偶发（附录屏）
环境：分辨率 / DPI / 库规模 / AI 配置状态
截图/录屏文件名：
```

### 16.2 执行节奏（v1.1 重排：建议 7 个工作日；SBTM 章程也可穿插进 Day 2~5 每天下午以压缩总周期）

| 阶段 | 内容 |
|---|---|
| Day 0 | 静态门禁摸底 K-0a~K-0d（约半天，可与 Day 1 上午合流） |
| Day 1 | 环境+素材包制作、基线五关（2.4）、模块 A 导入（A-17 万级 soak 素材拷贝可后台并行启动） |
| Day 2 | 模块 B UI 专项（U + R 全表）+ 模块 C 超级搜索 |
| Day 3 | 模块 D 标签系统 + 模块 E AI 打标 |
| Day 4 | 模块 F 查看器 + 模块 G 导出 + 模块 H 回收站 |
| Day 5 | 模块 I 设置与备份恢复 + 模块 J 新功能专项 + A-17 过夜验收 |
| Day 6 | 模块 L 联动全矩阵 |
| Day 7 | SBTM 章程×6 + 回归 + 出报告 |

### 16.3 回归纪律

- 修复后必回归三件套：①缺陷所在用例；②同模块相邻用例；③14.2 矩阵中该操作所在的整行（所有受影响面）
- P0 修复后加测一次 L-01 主链路闭环
- 修复交付前必须过 2.4 五关与第 13 章 K-0 门禁（K-10 把关铁律，v1.1 增补）
- 回归结果直接更新原缺陷条目状态（不新开）

---

## 17. 测试报告产出要求（测试结束后的交付物）

报告结构（按测试报告最佳实践）：

1. **执行摘要**：一段话结论 + 质量评级（A/B/C/D，按 1.5 标准）+ 是否放行
2. **测试范围与覆盖**：模块 × 用例数 × 执行率 × 通过率总表
3. **缺陷分析**：
   - 严重度分布表（P0~P3 × 数量 × 状态）
   - 模块分布热力（哪个模块缺陷最密集）
   - **三类核心问题专项结论**：A 类 / B 类 / C 类各一段（数量、典型代表、系统性风险判断）
   - **代码健康度（模块 K 专项结论，v1.1 增补）**：K-0 各静态基线数对照（clippy / fmt / unwrap 等是否恶化）、K-01~K-08 逐条结论与观察项、代码债务是否阻塞发布的判断
4. **遗留风险与已知问题核对**：附录 B 基线逐项状态（已修复/仍存在/恶化）
5. **上线建议**：放行 / 有条件放行（附条件清单）/ 阻塞（附阻塞项）

---

## 附录 A：快捷键速查（执行 Day 1 时实测补全）

| 快捷键 | 预期行为 | 实测 | 备注 |
|---|---|---|---|
| 1-5 | 设 1~5 星评级 | 待测 | 查看器/网格焦点态 |
| 0 | 清除评级 | 待测 | |
| F | 切换收藏 | 待测 | 以实际提示为准 |
| ←/→ | 查看‍器切换素材 | 待测 | |
| Esc | 关闭查看器/浮层 | 待测 | |
| 空格 | 视频播放/暂停 | 待测 | |

## 附录 B：已知问题基线核对表（对照 2026-08-31 评测报告）

| 编号 | 已知问题 | 本轮验证用例 | 状态 |
|---|---|---|---|
| S1 | 日志丢失 | I-08 | 待核 |
| S2 | V1 迁移非幂等 | I-09 | 待核 |
| S3 | 批次无熔断 | E-06 | 待核 |
| S4 | 配置双源 | I-02 | 待核 |
| S5~S10 | 其余稳定性缺陷（见原报告） | 按 TEST_STRATEGY §3.3 并发与性能、§3.4 数据完整性与崩溃恢复、§3.5 安全与隐私三个专项逐条核对；行为级条目借 A-14 / A-17 / E-10 复验（v1.1 补全责任归属） | 待核 |
| P1~P10 | 本地模型提示词问题 | E-11 | 待核 |
| 体验断点 | 高级 3 / 中级 6 / 低级 4 项 | 六件套对应用例（A-01/A-07/E-02/F-06/F-07/C-12） | 待核 |

## 附录 C：参考文档索引

| 文档 | 用途 |
|---|---|
| `docs/ARCHITECTURE.md` | 分层铁律与设计约束（短锁/不物理删除等） |
| `docs/prd_bagertea_v2.md` | R-01~R-33 需求与验收标准 |
| `docs/UI_DESIGN_SYSTEM.md` | UI 规范硬约束（R-08 依据） |
| `docs/review/super-search-contract-v2.md` | 超级搜索 V2 契约（降级/剔除/facet 语义） |
| `docs/review/facet-contract-v2.md` | 标签分面契约（三分支/级联/不变量） |
| `docs/review/超级搜索条件组前端UI改造方案-2026-09-06.md` | 三区 UI 定稿（§6.1 一行规则 / §6.4 预览 / §6.5 诊断 / §12 不做清单） |
| `docs/review/超级搜索三区改造与数值打标开发计划-2026-09-03.md` | must_not P0 修复 / 测试库重置流程 |
| `docs/review/全面评测报告-用户旅程模拟与技术健壮性-2026-08-31.md` | S/P 系列已知问题基线 |
| `docs/DEVELOPMENT.md` | 开发规范与铁律（模块 K 判定依据，v1.1 增补） |
| `docs/PERFORMANCE.md` | 性能红线与专项（F-13 考核线依据，v1.1 增补） |
| `docs/TEST_STRATEGY.md` / `docs/USER_TEST_GUIDE.md` | 自动化基线 / 早期走查参考 |

---

**本文档使用方法一句话版**：Day 0 完成静态门禁摸底（模块 K-0）、备齐 S1~S5 素材包、跑绿五关基线（2.4），然后按 Day 0~7 节奏逐表执行，边测边记，最后按第 17 章出报告。
