# 茶包素材 BagerTea AiMdeias V2 — 项目计划书

> 版本 v1.0 ｜ 2026-08-12 ｜ 基线：PRD v2.12 / PROGRESS 决策日志
> 定位：**项目管理视角的总纲**——回答"为什么做、做什么、做到哪了、接下来做什么、怎么算做好"。技术细节看 [ARCHITECTURE.md](ARCHITECTURE.md)，怎么开发看 [DEVELOPMENT.md](DEVELOPMENT.md)。

---

## 一、项目概述

| 项 | 内容 |
|---|---|
| 名称 | 茶包素材 BagerTea AiMdeias V2 |
| 定位 | 本地图片/视频素材数据库：入库托管 + 极速浏览 + 中文搜索 + 层级标签 + AI 打标 |
| 背景 | 推倒 V1 重做（V1 积累的性能与架构问题不可修复性高） |
| 技术栈 | Tauri 2 + Rust + SQLite FTS5 ｜ React 19 + Zustand + Tailwind v4 |
| 项目路径 | `F:\vibecode\chabaosucai` |
| 数据库 | `%APPDATA%\bagertea_ai_media_v2\library.db` |
| 体验基准 | 达芬奇底栏 + Notion 极简 + LR 式侧栏；**黑白灰高级感 + 苹果式简约动效** |

## 二、项目目标与成功标准

### 2.1 业务目标

1. 摄影/设计素材**集中托管**：总库/分库文件管理，入库即落库、可改名
2. **找得到**：3 万素材规模下滚动流畅、中文搜索即输即得、标签树导航
3. **打得快**：AI 云端打标 + 人工确认流，标签可自定义分类体系
4. **拿得出**：本地导出（未来网盘导出）

### 2.2 质量目标（每次交付必须满足）

| 维度 | 红线 |
|---|---|
| 稳定性 | `cargo test` 全绿 + `tsc --noEmit` 零报错 + `vite build` 通过，三关缺一不交 |
| 性能 | 缩略图解码走全局 imaging 引擎；JPG 320px 解码 ≤ 5ms；3 万素材滚动不卡（见 [PERFORMANCE.md](PERFORMANCE.md)） |
| 可维护性 | 分层解耦（commands→services→db），单文件单职责；**文档先行铁律**：先改文档再改代码 |
| 一致性 | UI 黑白灰、主 CTA 黑色实心/其余幽灵文字按钮；文档与实现不一致时以文档修正流程对齐 |

## 三、范围（Scope）

### 3.1 里程碑总览

| 阶段 | 内容 | 状态 |
|---|---|---|
| **M1 核心闭环** | 入库（两段式确认+总库/分库托管+批量改名）、网格浏览、双层缩略图、FTS5 中文搜索、父子层级标签、手动/云端 AI 打标、全屏查看器、本地导出、删除双策略、设置中心 | **代码已完成，出口走查中** |
| **M2 AI 与导出增强** | 本地小模型（下载断点续传+sha256）、百度网盘导出（OAuth）、夸克实验性、视频 AI 打标 | 未开始（T05b） |
| **M3 体验完善** | 批量增强、详情预览增强、标签管理（层级调整）、去重、回收站 | 远期 |
| **M4 远期** | 音频、多端同步、AI 语义搜索 | 远期 |

### 3.2 当前进度（2026-08-12）

- T01~T05a 全部完成；Rust 测试 37 全绿（lib 32 + 集成 5）
- UI 走查 v2.3~v2.12 共 12 轮老板反馈全部落地（详见 PRD 修订记录）
- 最新：v2.12 修复打标状态机 bug（done 可续跑 pending、空解析视为失败）

### 3.3 M1 出口标准（全部满足才算交付）

1. T01~T05a 任务看板全勾（PROGRESS.md 第二节）——**已完成**
2. PRD P0 需求（R-01~R-06、R-08~R-10、R-12~R-14、R-31~R-33）逐条走查通过——**进行中**
3. 3 万素材真实数据集性能回归——**待做**
4. 真实云端 API Key 端到端验证（建批→打标→确认→FTS 可搜）——**待做**（注意：需**支持视觉的模型**，mimo-v2.5 已实测不可用）

### 3.4 下一步计划

| 优先级 | 事项 | 说明 |
|---|---|---|
| P0 | 老板实测 AI 打标端到端 | 换视觉模型（qwen-vl-max / gpt-4o / claude-sonnet 级）复验 v2.12 修复 |
| P0 | M1 出口走查 | 按 3.3 清单逐条过 |
| P1 | T05b 开工 | 本地小模型 + 网盘导出（M2） |

## 四、风险登记（现行重点，全量见 PRD 第六章）

| 风险 | 等级 | 应对 |
|---|---|---|
| 中转站模型不支持视觉输入导致打标空结果 | 高 | v2.12 已改"空解析即失败"明示；实测环节必须换视觉模型验证 |
| API 成本失控 | 中 | 批量上限 500（settings.batch_limit）；「仅打标前 N 张」范围控制 |
| 3 万素材性能回归未实测 | 中 | M1 出口前必做；性能探针 `src-tauri/tests/perf_probe.rs` |
| 本地小模型选型未锁定（M2） | 中 | M2 排期前由架构评估给出「轻量/标准」两档默认方案 |
| 百度网盘 API 资质未确认（M2） | 中 | 二期排期前确认；受限则分片上传策略 |

## 五、协作与维护机制

1. **文档先行铁律**：任何偏离文档的实现决策，先记 PROGRESS.md 决策日志 + 改 PRD，再动代码
2. **单一事实来源**：每类信息只有一份权威文档（见 [README.md](README.md) 文档地图），禁止多头维护
3. **版本节奏**：PRD 修订号随老板走查反馈递增（当前 v2.12）；决策日志只追加不修改
4. **交接保障**：[HANDOVER.md](HANDOVER.md) 覆盖环境搭建→启动→发布→应急全链路，新人按此可独立上手

## 六、文档体系索引

| 文档 | 管辖范围 |
|---|---|
| [prd_bagertea_v2.md](prd_bagertea_v2.md) | 需求权威（P0-P3 需求池、UI 稿、风险） |
| [ARCHITECTURE.md](ARCHITECTURE.md) | **架构现状**（模块/数据流/关键机制，随代码同步） |
| [PROGRESS.md](PROGRESS.md) | 进度唯一事实来源 + 决策日志 |
| [DEVELOPMENT.md](DEVELOPMENT.md) | 开发规范与流程 |
| [PERFORMANCE.md](PERFORMANCE.md) | 性能数据与优化策略 |
| [TROUBLESHOOTING.md](TROUBLESHOOTING.md) | 踩坑记录与排查方法 |
| [HANDOVER.md](HANDOVER.md) | 交接与运维手册 |
